# The path to a first sale, judged on 2026-09-25

A read-only fleet (Workflow wf_1c74e0ed-28f, 43 agents) was asked, from the
repository at HEAD 5c985a0, for the shortest credible path from today's state
to a sellable vertical slice - three proposals from three stances (MVP-first,
risk-first, player-first), then one judge who re-read the repository before
deciding. STATUS: PROPOSED - the owner decides; ROADMAP section 27 carries the
compact form. Everything below is the agents' text, unedited: it is data, not
a decision, and its claims about the repository were checked by the judge, not
by the owner.

## The judge's synthesis

=== SUMMARY
STATE, verified at HEAD 5c985a0 (19.09b; 7 commits past the 9d1f81b the task named). Kernel: 14 modules (Tools/kernel_modules.txt), 109 add_test entries across Tests/, 18 frozen gates in Tools/run_gates.sh, 10 CI jobs (6 Linux presets, format, parse, windows-msvc-debug, macos) - windows-msvc-release is a preset (CMakePresets.json:150) built by no job. Engine: FIVE engine modules (parse_engine_modules.py:56: VaelenPresentation, Vaelen, VaelenGame, VaelenUI, VaelenWalk - not the three the task text named), 41 files, 24 moved or new since the last complete UBT build b0921=867a129, 112 shim beliefs (check_shim_ledger.py), check_engine_status.py clean. Ledger Tools/engine_builds.txt has five rows and NO `s1`; Tests/Run/Sessions holds only the 09-16 pair; Tests/CMakeLists.txt:140 has Session.SelfTest only - so STATUS.md's "S1 built 19.03b on 2026-09-25" and ROADMAP:8193 mean COMPILED, not closed: no rev-parse, no log, none of steps 2-6. The engine host (VaelenWorldSubsystem.cpp:141-155) asks Play+Stream only and forces Rules.WantBound=0 (also :620 on Load); DefaultEngine.ini still carries Phase 00's provisional Lumen GI/reflections, Nanite, VT and distance fields plus GameDefaultMap=/Engine/Maps/Entry and GlobalDefaultGameMode=VaelenUI.VaelenGameMode; DefaultGame.ini has ProjectVersion 0.0.1, CompanyName empty, PPBC_Development; Content/ is .gitkeep, .gitattributes marks *.uasset binary with no LFS; /Game/M_VaelenTile is FObjectFinder'd at VaelenLand.cpp:38 and VaelenViewActor.cpp:331 and /Engine/BasicShapes is loaded by string at VaelenAtlasActor.cpp:351 and VaelenViewActor.cpp:312; the Game target (Source/Vaelen.Target.cs, monolithic) has never been linked in any configuration; no package has ever been made; the console commands the game is driven by (Vaelen.Play/Walk/Save/Load/Saves/Stream.Write) are the only start path. The store keeps `<name>.writing` but the `.previous` rename-aside is not built (VaelenCheckpointStore.cpp:75; ROADMAP:8172-8189). ADR-0155..0159 are PROPOSED (DECISIONS.md:10660-10762); check_session.py has no --frame-target/--baseline yet although clause (n) names them. MEASURED HERE (Debug Atlas, this container): 128/300+120 Play 36.6 s, Play+Stream ~39 s, Play+Colony 42 s (layout `pits 1`); Phase 16's cell WITH Lively over the pre-play years is 20.3 min, 3.3 GB RSS, 2.37 GB container (ROADMAP:5221-5230), 61 s/408 MiB even at 60+30 (ROADMAP:5273) - and Phase 12's 2m05s (ROADMAP:3043) is the PLAY of 14400 days, not the Begin, so proposal 3's "Lively is affordable" conflates the two. THE FINDING NONE OF THE THREE HAD: the premise flip every proposal wrote (Asked.Colony=true + WantBound=1) offers NOBODY at the 128 seed - five cells replayed here: Play/WantBound 1 -> Erdass (3610, region 2) bound to Okigerdo; Play+Stream (the host's wiring)/WantBound 1 -> Esvuhurdu (3580, region 2) bound to Arorde; Play+Colony/WantBound 0 -> Lebut (3200, region 75) free; Play+Colony/WantBound 1 -> nobody; Play+Colony+Stream/WantBound 1 -> nobody. BeginEnslaved (Start.cpp:93) needs a live BondState that is not Free among ages 16-40; with the colony wired those persons are not offered (hypotheses: the pit is founded on Run_.Detail, Aelvor.cpp:451, so Phase 15's detail choice leaves region 2 at count grain; the founding hands are manumitted at 25/1000 a year over 120 years, Bondage.h:67, Holders.h). Also: Atlas `--stand` sets `RO.Play = true; // and nothing else` (Main.cpp:3044), so a colony stand-in cannot be written today. A bound start therefore costs one line NOW (WantBound=1 in the host's own wiring); "in the mining colony" costs a kernel question with a 40-second instrument and an unknown answer, and LivelyFrom=play does not rescue it (pre-play years would be the same colony world). THESIS (all three agree, verified): the owner's chair is the critical path - six build days in 18 (09-07..09-25), 1-3 defects per engine build that nothing headless can see, ~20 headless commits a day. The shortest credible path: close S1 on the build that already compiled, run S2 exactly as pinned (19d6a6a) with one report-only Game-target compile appended, land the product layer headless so S3 and S4 each carry a piece of it (bound start, LivelyFrom measured, renderer config measured first, WalkKeys, a key-driven front end with autosave and the rename-aside, cook config with a known-answer check), live S4's month in the first UAT Development package from the title page, playtest it on 2-3 strangers, then one Shipping sitting replayed to the same digests and uploaded to Steam Early Access priced and described as exactly what it is: one bound life in a procedurally generated living world walked in abstract shapes, keyboard only, no audio, 128 tiles, the day on Space, saves per build. Deferred past the first sale: modding, the 256 stress test, the v4 migration chain, Infrastructure in the played world, the "freedom has a price" kernel rule, animated characters and audio unless a stranger names the look as the blocker.

=== ORDER
 - 0. TODAY, HEADLESS (0.5 d): the S1-close kit, so the log is adopted the day it lands. Tests/Run/Sessions/s1.session with `head` and step-4 lines against `VaelenAtlas --gate <stream> --want-bound 0`, a `Session.P19S1` add_test beside Session.SelfTest (Tests/CMakeLists.txt:140), the `s1` RECORDED row template; ENGINE_HANDOFF 'PHASE 19 - sitting S1' amended: NO REBUILD - steps 2-6 on the 19.03b checkout already built, rev-parse first - and at step 5 the renderer A/B proposal 2 asked for, typed at the console over the same ground-level view: `stat unit`, `stat gpu`, then `r.DynamicGlobalIlluminationMethod 0`, `r.ReflectionMethod 0`, `r.VolumetricCloud 0`, `sg.PostProcessQuality 1`, `r.ScreenPercentage 75`, read again, and `r.RHIName` (the T400 runs D3D11, driver 472.47, ROADMAP:3692 - Lumen may already be silently off, in which case the 28.15 ms is the Entry map's sky, clouds and post and the fix is another). The five premise cells measured above go into ROADMAP as a table so step 2 is decided by measurement, not by the plan's prose. ACCEPT: the ask sent; `ctest -R '^Session\.SelfTest$'` still red on purpose. WHY: one hour of the owner closes 16.14's engine half and Phase 18's engine line at zero build cost, and the frame-time unknown becomes two numbers instead of a guess.
 - 1. S1-CLOSE (owner, ~1 h, no build): steps 2-6 + the A/B, whole Saved/Logs/Vaelen.log back. Headless same day: log committed as s1-<date>.log, `s1` RECORDED, check_engine_status.py moves the seven files, beliefs -> SEEN. ACCEPT: `ctest -R '^Session\.P19S1$'` green; `frame ec18241b89c3d246, ground 8f7f4948f49b6e86` printed by an engine; loaded state digest == saved; host24 check ends `--no-climate`; the two stat unit readings in ROADMAP; if the A/B wins, the config change is written now (committed in step 6). WHY: it is the baseline every later clause (n) reading is measured against and the first evidence of a sitting that the reader can refuse.
 - 2. P-01 THE BOUND START, AS THE WORLD OFFERS IT TODAY (headless, 0.5 d; engine file, so it lands AFTER S2's pinned commit 19d6a6a and before S3's): VaelenWorldSubsystem.cpp:155 `Rules.WantBound = 1`, falling back to 0 with a printed `LogVaelenPlay: nobody bound to be; taking whoever is there` (BeginEnslaved returns 0 legitimately, ROADMAP:3866); the same at :620; `Asked.Colony` stays FALSE for now (measured: the colony wiring offers nobody bound at 128). Atlas `--stand` made to take RO.Colony/Stream/Lively like `--replay` does (Main.cpp:3044 vs :1799), so a stand-in exists for every wiring; a new stand-in written with `--stream --want-bound 1` pinned as `Replay.Bound` through ReplayPlayed.cmake (four digests; here: Esvuhurdu, person 3580, region 2, state 4a63d33abe32c64e, log 3030eff3311f653b, life 80714d8b1b6ff8c4, panel fe133907762579b1 - re-pinned once in step 6 if the scale moves); `Run.Walk` (Test_Walk.cpp:57 WalkOptions) gains a WantBound=1 cell. ACCEPT: the replayed page's Self row reads `bound to Arorde since year 420` (Panel.cpp:391); Wrong 0, every look == Life.Region; `frozen_census.py --diff` 0 removed (host options move no WORLD pin). WHY: the pitch's first word is 'enslaved' and it costs one line; the colony is the second word and costs an answer nobody has.
 - 3. P-01b THE COLONY QUESTION (headless, timebox 1 d, kernel READ first): why Play+Colony(+Stream) with WantBound=1 offers nobody at 128/300+120 while Play alone offers two. Instrument: the 40-second Atlas cell over Years 20/60/120, --stream on/off, and the colony's Hands at play start (Net.h:57), the table into ROADMAP. If a HOST-side answer exists (fewer play years; the bound region detailed beside the colony at Begin; StartRules that reach the colony's hands) P-01 takes it and `Asked.Colony = true` (+4.7 s, `pits 1` in the layout, HeadlessCheck already prints `--colony`, :488). If it needs a kernel rule (the colony re-binding its hands, Holders.h) it moves Colony gates and is a re-freeze: it goes AFTER the first sale, the slice ships 'bound, the colony a place in the world you are not in' or 'bound, no colony', and the store page says which. WHY: a decision the plan wrote as two lines is a measurement; proposal 2 alone foresaw it.
 - 4. S2 (owner, 19.06 at commit 19d6a6a found by subject, EXACTLY as ENGINE_HANDOFF 'sitting S2' writes it, budget 2 sittings) PLUS one appended report-only step 11: `Build.bat Vaelen Win64 Development` - the Game target's first link ever (monolithic, `*_API` empty; the C2487 class was DLL-only and Kernel.DllApi says it does not see the rest) - Result line and first error only. Headless: Session.P19S2 reader, `s2` row; every error -> shim line corrected -> `python3 Tools/test_engine_shim.py && Tools/verify_fast.sh && ctest -R '^Kernel\.'` (CI run 309's lesson) -> fresh pinned commit -> retake. Fallbacks fixed before the sitting: BindAxisKey if runtime mapping contexts are refused (ADR-0157:23), a UStaticMesh built from the same int32 arrays if PMC collision fails under CharacterMovement, a committed material only if GEngine->VertexColorMaterial is refused. ACCEPT: `ctest -R '^Session\.P19S2$'`: terrain line byte-equal to `VaelenAtlas --size 128 --prehistory 300 --years 120 --scene-terrain R`, climate line byte-equal, probe <= 1.0 cm with 0 misses and the forced bias printing 5.0, four move lines (0,1)(-1,0)(0,-1)(1,0); a UBT Result line for the Game target in the committed log. WHY: 11 new engine files, 94 of the 112 beliefs, and every API the product stands on run once here; nothing is built on them before this.
 - 5. HEADLESS WHILE S2 WAITS (the S2 fix loop pre-empts everything): (a) 19.11/19.12's headless halves as the plan rows say - AVaelenScenery ISMs from /Engine/BasicShapes (houses Cube+Cone with collision, figures Cylinder+Sphere, square, roads, pit), AVaelenLand repaint on OnViewsTaken, WalkKeys on the walk controller with M -> Scene::CrossingOf and Speak/Give/Take -> Scene::AimAt (Tab fallback), the one-a-day `LogVaelenWalk: day D tile T region R life L looked K`, `Vaelen.Scene`, the LogVaelenWalk summary in Stream.Write, ReplayPlayed.cmake driver `Replay.OnFoot` (-DERA=--climate, -DMORE='--stream --keys TWREMFGK'), `Run.Gate.OnFoot`, gates 19-20 in run_gates.sh:44-47, Session.P19S3/S4 readers, and check_session.py's `--frame-target`/`--baseline` that clause (n) names and which do not exist. (b) P-02 `Run::Options::LivelyFrom` (a year; 0 = today): Living/Repute/Fame/Judgement DECLARED at wiring, RUN from year >= LivelyFrom; HOST byte 25 -> 26 in the 18.02 form (refused by name on adopt, a 25-byte HOST still reads); Atlas `--lively-from N`; the host asks for PreHistory+Years. PREDICT IN WRITING FIRST, then measure: Begin at 128/300+120 <= 1.2x Play-only (~44 s Debug here) and RSS <= 200 MiB; 90 played days at 128 yield >= 1 hearsay reaching the played person, >= 1 name carried down a road, judgements > 0. If the prediction fails, ship Play(+Colony) only and the store page says 'sandbox', not 'RPG'. Note in the ADR that LivelyFrom does not create a bound start: the pre-play world is unchanged by it. (c) `windows-msvc-release` added to kernel-ci.yml (the preset exists at CMakePresets.json:150; the kernel is integer-only under R8, so release digests should hold - proven before any Shipping exe is trusted). ACCEPT: parse 0 errors; Kernel.UiFence/EngineStatus/ShimLedger/ModuleLists green after every engine-code commit; `Run.LivelyFromPlay` with the bounds; `Run.Checkpoint` gains TheLivelyFromIsInTheHostSection; census 0 removed. WHY: S3 and S4 need code that is a plan row today, and Phase 12 is the only machinery that makes the world react to a name - at 20 minutes and 3.3 GB per new game as wired, it cannot ship any other way.
 - 6. THE ONE COUNTED WORLD-MOVING COMMIT BEFORE S3 (headless, 1 d, the 18.10 form, prediction table first, census after): Q1 scale decided from S2's walk - the first time anyone has crossed a region at 250 m tiles - keep `SceneScale{25000, 250}` unless the owner says seven minutes is too long, then `{10000, 100}` (same 1/1000 ratio, steep still 0 at 128 and 256); Q2 `Keys_ = WalkKeys` (Speak on F) and the arrows; Q3 M at the fence as built; Q9 `GlobalDefaultGameMode=/Script/VaelenWalk.VaelenWalkGameMode`; the renderer config S1-close measured (r.DynamicGlobalIlluminationMethod=0, r.ReflectionMethod=0, Nanite and VT off, a DefaultScalability.ini pinning sg.*) with the renderer ADR Phase 13 owed; `Replay.Bound` re-pinned once with LivelyFrom if (b) passed. ACCEPT: Atlas.SceneTerrain128/SceneLayout128/SceneSky128, Replay.Climate's scene lines and View.Panel's walk page re-pinned ONCE and only if the scale moved; `frozen_census.py --diff` shows exactly the predicted WORLD/terrain, layout, sky, panel rows and 0 GEN/HASH. WHY: three separate re-freezes in a week is how ADR-0095 happened, and the scale must be settled before S3 records the first replayed scene.
 - 7. S3 (owner, 19.11 as written, pinned by subject, budget 2): now a bound life, the scene from the played world (not the Phase 13 actor's second instance, VaelenViewActor.cpp:372-396), both seasons, four `blocked` lines, `Vaelen.Stream.Write`, the `Vaelen.View` control, and `stat unit` at eye level in the settlement with the config committed - clause (n)'s figure. ACCEPT: `ctest -R '^Session\.P19S3$'`: `VaelenAtlas --replay <S3 stream> --panel --want-bound 1 --stream --keys TWREMFGK --scene [--colony] [--lively-from N]` prints the Scene, Climate and Play lines byte-identical as files; every LogVaelenWalk line has region == life == looked; ISM instance counts == the layout line's; eye-level frame time not worse than S1-close's baseline (propose <= 33 ms on the T400 as the floor). WHY: the first sitting where what the owner sees is the played world with people in it, and the reading that decides Q4.
 - 8. HEADLESS FOR S4 (P-03 + P-04, 3-4 d): the front end in VaelenUI as Canvas text pages in the 14.09 style, no UMG, no Blueprint - title: [N]ew life (128/120), [C]ontinue (the newest usable name from Saves()), [Q]uit; Esc in play: [S]ave/[L]oad/[Q]uit; F5 = Save('auto'), F8 = Load('auto'); an 'AELVOR is being made' page drawn BEFORE a BLOCKING Begin (no thread: Begin is ~1-2 s in Development by the debug multiple, unmeasured on the owner's machine - measured at S4); autosave every 10 day turns under `auto`, from AdvanceDay, never wall clock (ADR-0109); the `.previous` rename-aside in FVaelenCheckpointStore and the kernel Store via IsUsableCheckpointName (Store.h:130) + `Run.SaveAside` (a write over an existing name keeps `<name>.previous` hidden from listings; Read of a missing name restores it; CannotWrite leaves both); a second PanelKeys table for QWERTY (W is Work and ValidKeys names the clash, so Work moves) with a title-page toggle - no pin moves. P-04: DefaultGame.ini `+DirectoriesToAlwaysCook=(Path="/Engine/BasicShapes")`, `+MapsToCook=(FilePath="/Engine/Maps/Entry")`, ProjectVersion 0.1.0, CompanyName; the M_VaelenTile FObjectFinders (VaelenLand.cpp:38, VaelenViewActor.cpp:331) removed - vertex colours are the shipped look; `Tools/check_cook.py` (`Kernel.Cook`): every /Engine/ or /Game/ path a Source/ file names must be under a cooked directory or map, known answer at HEAD (BasicShapes/Cube, BasicShapeMaterial, WorldGridMaterial, EngineDebugMaterials/VertexColorMaterial, Maps/Entry; M_VaelenTile refused); ENGINE_HANDOFF gains `RunUAT BuildCookRun -project=<path>\Vaelen.uproject -platform=Win64 -clientconfig=Development -build -cook -stage -pak -archive -archivedirectory=<dir>` and what to bring back. Plus `Run.Soak` print-only: 1080 day turns at 128 in the shipped wiring, ms/day, RSS and container size at days 1/360/720/1080, a ceiling decided in git (propose day turn < 50 ms, RSS < 2 GB, save < 100 MB at 3 years); over it, a refusal by size before sale, the PlayerActed log cut (54% of the log, 17.05) after it. ACCEPT: parse clean; Kernel.UiFence, Kernel.EngineStatus (new files UNVERIFIED), Kernel.Cook, Run.SaveAside green; the soak's four figures in ROADMAP. WHY: a product starts, continues and quits without a console (Shipping has none), a save that can vanish is the one defect that ends an EA run, and uncooked engine content is the class of failure that costs a whole sitting to find on the owner's machine.
 - 9. S4 (owner, 19.12 + P-03/P-04 seen, budget 2): OPEN with the UAT Development line - bring back its result and the first error; if it packages, the life is lived in the archived exe from the title page: [N]ew life, 30+ days on foot, every verb, >= 2 crossings by M, >= 1 refused, >= 1 Speak aimed at a figure, into the first frosts, F5, [Q]uit, relaunch, [C]ontinue, 5 more days, `Vaelen.Stream.Write` (Development keeps the console), a screenshot whose last row is the panel digest, `stat unit`, the container size and process RSS after 30 days, seconds from launch to standing. If it does not package, S4 is 19.12 in the editor -game exactly as the plan row says and packaging becomes a sitting of its own. ACCEPT: `Replay.OnFoot` and `Run.Gate.OnFoot` (N of N answered identically, D of D days, P == 0, C >= 2, A >= 1); `Session.P19S4`; `Run.Gate.Saved` over the package's container (closes 16.14 for good); the Continue's `loaded` digest == the autosave's; the stream committed as Tests/Run/Streams/aelvor128-onfoot-<date>.stream; Phase 19 read clause by clause (a)-(n) with the CI run id, STATUS moved by the checker, ADR-0155..0159 applied. WHY: the first sale needs one life lived by a person in the binary a stranger will run, and the replay proves that binary is the world the CI holds.
 - 10. IN PARALLEL FROM S2 ON (owner's hours, no code): Steamworks partner account and the app fee; a store page that says exactly what the build is (abstract shapes, keyboard only, English, no audio, 128 tiles, the day on a key, saves may be refused by name between builds); CompanyName, EULA/privacy text, the name checked. AFTER S4: the Development package to 2-3 strangers, one evening each, one question - would you pay for this - the answers and their streams into ROADMAP (their streams must replay). WHY: everything before this is engineering; this is the only evidence for 'would a stranger pay', and Steam review takes about a week that overlaps the sittings.
 - 11. S5 THE SHIPPING PACKAGE AND THE UPLOAD (owner, 1 sitting): the same UAT line with `-clientconfig=Shipping` (console gone - the front end must suffice; DO_CHECK and VAELEN_ASSERTS_ENABLED=0 per VaelenCore.Build.cs:46-52 - the noasserts CI presets already model the kernel under it), the archived exe run on a second Windows machine or a user account without the editor, a 30-minute life, its stream replayed headless to the same four digests (`Session.P19S5`), the pak containing only what check_cook predicted, then the depot upload, a low price, Early Access. OPTIONAL C1 (one capped sitting, only if the stranger playtest names the look as the blocker, never before the upload path works): Git LFS for *.uasset/*.umap/*.wav, the UE Mannequin on AVaelenWalker by object path, three CC0 sounds by path; the VertexColorMaterial and basic-shape fallbacks stay so a clone without LFS still runs; ship the prototype look if the cap is hit. PARALLEL AND OPTIONAL FROM DAY ONE (proposal 3's best idea, nothing waits on it): a self-hosted GitHub Actions runner on the owner's PC running `Build.bat VaelenEditor Win64 Development` and `Build.bat Vaelen Win64 Development` on every push - 1 h of the owner, the workflow written headless - so a compile error costs a push and not a sitting; its acceptance is a planted `*_API` on a member failing the runner with the C2487 line while Kernel.DllApi refuses it too.

=== CUTS
 - Phase 20 STRESS TEST as planned (256 maps, 500-year runs, log tiering over 49 Log().All() sites, ~178 frozen literals): reduced to the print-only 3-year Run.Soak of step 8 at 128 in the shipped wiring; ship 128 only.
 - Phase 21 MODDING entirely: stable ids already exist (ADR-0004); data-driven definitions and a mod loader sell to nobody before there is a game.
 - Phase 22 POLISH except the four ship items pulled forward (title/pause pages, loading page, save keys + autosave + rename-aside, cook/package config): no balance pass, no settings menu beyond a scalability preset, no save browser/thumbnails, no localisation (the page is composed and digested kernel-side; a French page moves every panel pin and is its own post-sale phase), no audio and no animated characters unless the stranger playtest names them (optional C1, after the upload path works), no achievements/cloud/Steamworks SDK code (a Steam depot ships an exe without any of them), no controller.
 - Phase 16's migration half (the v4 bump, Save.CorpusMigrates, the forge): ship v3 + HOST containers; 16.08 already refuses an incompatible save BY NAME and never half-loads; 'saves are per build until 1.0' is the EA policy, the migration chain the first post-sale kernel task.
 - Lively over the 120 pre-play years: never (20.3 min, 3.3 GB RSS, 2.37 GB container at 128 - ROADMAP:5221-5230). Either LivelyFrom = play (step 5b, gated by its own written prediction) or Lively off; the decision is a measurement.
 - Proposal 3's 19.14 'freedom has a price' (Give to the holder -> manumission, Move while bound -> flight and judgement): DEFERRED past the first sale. It is a kernel gameplay rule with a re-freeze (Player/Gameplay gates, Run.Golden), 4-5 headless days, on a premise cell (bound in the colony) that does not exist at 128 today; the stranger playtest decides whether it is the first post-sale kernel task. Kept in ROADMAP as the named candidate.
 - Proposal 3's 19.15 kernel-side Menu leaf (a View leaf digested like Panel.h, View.Menu pin): a menu is host UI; a Canvas page in VaelenUI, parsed by the same job, costs no kernel module change and no pin.
 - Proposal 1's async Begin on a thread (Async(EAsyncExecution::Thread) owning the Aelvor): a new class of defect nothing headless sees, for a 1-2 s wait in Development; a blocking Begin behind a drawn page instead, measured at S4.
 - Proposal 2's committed project map Content/Maps/Walk.umap and Git LFS before the first package: asset-free stays (ADR-0157 is what lets the CI see the engine code); /Engine/Maps/Entry with the C++ sky is cooked by +MapsToCook; LFS only if C1 happens.
 - Proposal 1's scale change to 100 m tiles before S3: not before anyone has walked a region; decided from S2's walk and folded into step 6's one counted commit if it moves.
 - Everything ROADMAP section 26 already lists as NOT in Phase 19, kept out before the sale: VaelenInfrastructure in the played world (Q7 - its own re-freeze + HOST byte, and it holds no house, Buildings.h:44-51), a kernel position below the region, travel within a day or an automatic day (Q8: the day stays on Space - it is what makes a walk a replayable stream), skeletal NPCs/Landscape/Nanite/World Partition/Lumen, NPCs walking, a water leaf, saving the walker's position, a fifth wiring, new verbs/combat/inventory (Panel.h:43 fixes eight), re-recording the 09-16 and 09-21 months.
 - The Phase 13 map actors (AVaelenViewActor/AVaelenAtlasActor, a second world instance, 72 K instances, 30.4 ms at ground level): compiled for the sittings' control line and the digests, never spawned by the front end, out of the shipped loop.
 - 256-tile worlds in the shipped build, the /Game/M_VaelenTile asset that lives on one disk, UMG/Slate/Blueprints.
 - Merging sittings rather than adding them: S3 and S4 carry the product checks; the Phase 19 budget of six stays the ceiling for S1-close..S4, with S5 (Shipping) and C1 (optional) outside it. ADR-0129 and ADR-0131 stay open.

=== RISKS
 - Engine defects nothing headless can see: 24 engine files never compiled by UBT, 112 shim beliefs (94 for the walk), ProceduralMeshComponent complex collision under CharacterMovement, runtime Enhanced Input mapping contexts, ZQSD following the AZERTY layout, a C++ sky in a lightless map, GEngine->VertexColorMaterial, FKey(FName), the monolithic Game target never linked, and DLL-only error classes beyond C2487 that Kernel.DllApi says it does not see. History: three fixes at 14.08, S1's first attempt stopped at step 1. => Sittings pinned to a commit found by subject, rev-parse first, first error only, REPORT not repair; S2 runs every belief once and is budgeted two sittings; ADR-0157's fallbacks (BindAxisKey; a UStaticMesh from the same int32 arrays; a committed material) decided before the sitting so a refusal redesigns the engine side only; `ctest -R '^Kernel\.'` after every engine-code commit; nothing engine-side lands between two sittings except what the next sitting's commit names; the optional self-hosted UBT runner turns compile errors into pushes.
 - The premise is not in the world as measured: with the colony wired, WantBound=1 offers nobody at the 128 seed (five cells replayed here); the cause is a hypothesis (the pit founded on Run_.Detail, Aelvor.cpp:451; founding hands manumitted at 25/1000 a year, Bondage.h:67) and the cheapest fix may be a kernel rule that moves Colony gates. LivelyFrom = play does not change the pre-play world and so does not rescue it. The Atlas `--stand` cannot even write a colony stand-in today (Main.cpp:3044). => Step 2 ships the bound start that exists today at zero cost (Esvuhurdu bound to Arorde in the host's own wiring); step 3 is a 1-day timebox with a 40-second instrument and a table in ROADMAP; a kernel answer is scheduled after the first sale; the store page says whether the colony is where you start or a place you can reach; `--stand` is fixed in step 2 so every wiring has a stand-in.
 - The fun is unproven and the world may not react: after 30 days the page reads 'X ate, 1 hour of it' sixteen times (verified here on Lebut's page); Phase 12 closed with 'the world gives the reputation machinery nothing to carry' (ROADMAP:2586); a bound player's fate changes only by yearly dice (ManumissionPerMille 25, FlightPerMille 8). A stranger pays for consequence. => LivelyFrom (step 5b) with a written prediction and a hearsay/judgement acceptance puts opinions and a judged name on the page from the first played day; the owner lives 30+ days in the S4 package and writes a verdict; 2-3 strangers play the Development package before upload and their answer is recorded; if the prediction fails the page and price say 'sandbox'; the freedom rule is the named first post-sale kernel task if the playtest asks for an arc.
 - Frame time on the owner's T400 4 GB (D3D11, driver 472.47): 30.4 ms GPU-bound at ground level over the 13.09 scene, 28.15 ms for the template map with the HUD alone (ROADMAP:4641); DefaultEngine.ini enables Lumen GI/reflections, Nanite, VT and distance fields as a Phase 00 'provisional' no ADR revisited - and on D3D11 Lumen may already be off, so the 28 ms may be the sky/clouds/post; the walk uploads 1,065,024 vertices and 2,097,152 triangles at 128 plus ISMs for ~7,000 houses and ~1,400-2,000 figures. => S1-close's console A/B decides by measurement at zero code cost; the winning config lands in step 6 with the renderer ADR; S2 and S3 each bring `stat unit` at eye level and check_session.py gains --frame-target/--baseline; clause (n) gets a measured floor (<= 33 ms on the T400 proposed, Steam min-spec declared from the S3 reading); if still GPU-bound, cull figures beyond the played region and its ring (a Layout/AVaelenScenery rule, no kernel change) and drop far chunks to stride 8 as built.
 - Packaging has never been done: engine content loaded by string path (/Engine/BasicShapes, engine materials, /Engine/Maps/Entry as GameDefaultMap) is not cooked unless listed; the M_VaelenTile FObjectFinder logs an error on every start without the asset; Shipping compiles the console out and every command the game is driven by today is a console command; DO_CHECK and the kernel's asserts are 0 in Shipping; MSVC release is built by no CI job so a release exe could replay to other digests. => Step 8's check_cook.py with a known answer at HEAD; Development package first (console kept for diagnosis), Shipping second; every path becomes a key in the front end; the noasserts CI presets prove the kernel runs with checks off; windows-msvc-release added to CI before S5; S5's acceptance is the packaged build's stream replaying headless to the same four digests; the UAT line and what to bring back written into ENGINE_HANDOFF; S4 falls back to the editor if the first UAT fails so the month on foot is not lost.
 - Save size and memory over a long life: the event log is 112 B/event and three quarters of every image, Phase 16 truncates nothing, the log grows 49-285 kB per simulated year, a 95-second rollback copy on every load doubles peak memory at the Lively cell, and nobody has run the engine past 142 days. => LivelyFrom = play keeps the pre-play log at the Play-only size (~22 MB at 128); Run.Soak prints ms/day, RSS and container size at 1/360/720/1080 days in the shipped wiring and a ceiling is decided in git before the sale; S4 measures the container and RSS after 30 days; autosave every 10 day turns bounds a player's loss; a hard refusal by name above a stated size rather than truncation; the PlayerActed log cut is a post-sale re-freeze with a versioned save break.
 - Save safety and compatibility across Early Access updates: the engine store's replace could still lose the last good save by NAME after a failed rename (ROADMAP:8172-8189, `.previous` unbuilt); 16.14 has never run on the engine; v3 containers with no migration chain, and any type added, removed or resized refuses every existing save. => S1-close runs the 16.14 steps; step 8 builds the rename-aside in both stores with Run.SaveAside failing on purpose, plus autosave; S4 exercises Save/Quit/Continue in the package and Run.Gate.Saved adopts its container; 16.08's refusal by name means a save is never half-applied; the EA notes say saves are per build; VAELEN_SAVE_FORMAT_VERSION discipline and the census stay; the migration chain is the first post-sale kernel task.
 - The owner is the critical path: one sitting per ~3 days, each a build-report-fix loop; an unmeasured clause (14.10's fps) or a missing verb (15.10's TakeSomebodyElse) has cost a week; and S1's evidence is incomplete - 19.03b compiled but no log came back, so Phase 16's and Phase 18's engine lines and the clause (n) baseline do not exist. => The first ask is steps 2-6 on the build that already compiled (one hour, no build); each sitting's section lists exact commands and the lines to bring back, is <= 2 h, and the reader refuses a log without its rev-parse; S3 and S4 carry the product checks so no sitting is only verification; the session entry and ledger row are written the day a log lands; the optional runner moves compile errors off the chair.
 - The discipline machinery slows product decisions and can retire a gate silently: the scale, the keys, the renderer config and LivelyFrom each move pins or add a HOST byte; the gates take 67 minutes; three re-freezes in a week is the ADR-0095 pattern; 18.10's CI run 297 lesson (pins written as prose in CMake strings escaped the census). => Step 6 batches every world-moving decision into one counted commit in the 18.10 form (prediction table first, census after); LivelyFrom is a host-asked option (default 0) so no existing pin moves; the gates run at that head and at S4's, the touched suites and the census at every commit; `frozen_census.py --diff` 0 removed is in every acceptance.
 - Determinism across the engine boundary in the walk: float capsule positions meet integer fences (Scene::Inside, RegionAt) every step, and a look recorded from the wrong region changes the world (ROADMAP:4950-4958). => 19.07's Run.Walk already proves the body is not an input headless (two wanderings, one stream); S3/S4 require region == life == looked on every day line and P == 0 put-backs; the probe's forced 5.0 cm bias proves the probe can fail; step 2 adds the WantBound=1 cell to Run.Walk.
 - LivelyFrom's prediction may fail (Begin > 1.2x Play-only, RSS > 200 MiB, or no hearsay reaches the played person in 90 days at 128 because the living rate is 20/1000 and nobody moves), and a HOST byte is a counted commit of its own. => The prediction is written before the first run and the acceptance names the bounds; on failure the slice ships Play(+Colony) only, saves 2 headless days, changes nothing else, and the store page says 'sandbox'.
 - Commercial and legal debt nothing in the repo covers: CompanyName empty, ProjectVersion 0.0.1, no EULA or privacy text, the name VAELEN unchecked, the Steam fee and review time, and no second Windows machine measured (the T400 i5-10400 is the only engine machine and is below a typical min spec). => Step 10 is an owner checklist with dates started at S2, not after S4; the min spec is declared from the S3 reading and S5's second-machine run; CompanyName and ProjectVersion land in step 8.

=== ESTIMATE
OWNER SITTINGS (1-3 h each plus the report): S1-close 1 (~1 h, no build); S2 1-2 (11 new engine files, 94 beliefs, PMC + Enhanced Input first run, the Game target's first link appended); S3 1-2 (scenery, WalkKeys, bound start, renderer config, the frame reading); S4 1-2 (first UAT Development package, front end, a 30-day life, save/continue, the on-foot gate; the editor fallback if UAT fails costs the second sitting); S5 1 (Shipping package on a second machine, upload); optional C1 1 (Mannequin + sounds under LFS, capped). TOTAL 5-8 planned, median 6-7, plus 1 optional - S1-close..S4 inside the plan's Phase 19 budget of six, S5 and C1 outside it; the owner's runner setup is ~1 h and not a sitting. HEADLESS: S1-close kit 0.5 d; P-01 bound start + --stand fix + Replay.Bound 0.5 d; P-01b colony question 1 d timebox; 19.11/19.12 halves + S2-S4 readers + check_session flags 2.5 d; P-02 LivelyFrom (option, HOST byte, prediction, measurement, re-pin) 2-3 d; the counted commit 1 d; P-03 front end + autosave + rename-aside + QWERTY table 2-3 d; P-04 cook config + check_cook + UAT line 1 d; Run.Soak 1 d; windows-msvc-release leg + runner workflow 0.5 d; fix loops after each sitting 0.5-1 d x 4-5; docs/ADRs/STATUS/EA notes 1 d; TOTAL 15-19 working days = 3-4 headless weeks, none on the critical path except the fix loops (each <= 1 d by the standing rule). CALENDAR: at the observed cadence (six build days in 18) with the owner sitting twice a week and the store page done in parallel from S2, the first Steam Early Access upload is 5-7 weeks from 2026-09-25 (early to mid November 2026); at one sitting a week, 9-11 weeks; +1-2 weeks if C1 happens. ASSUMPTIONS: the owner sits >= 2x/week and each sitting is a written script of <= 2 h; S2 needs <= 2 sittings and no UE API refusal forces a redesign beyond ADR-0157's named fallbacks; P-02's prediction holds (else Play-only ships, -2 d); the T400 reads <= 33 ms at eye level after the measured config (else one culling task, ~1 d + the S3 reading); the colony question has a host-side answer or is deferred (either way 1 d); the Game target links in Development on the first or second try; the cooker takes /Engine/BasicShapes and Entry from the listed config (else the committed-map fallback, 0.5 d + a retake); Begin in Development at 128 is 1-2 s (inferred from the debug multiple, unmeasured on the owner's machine); Steam review ~1 week overlapping the sittings; no audio, animation, localisation or settings before the sale; the stranger playtest (2-3 people, one evening each, in the S4 week) is the only evidence for 'would pay'. NOT COUNTED: the owner's hours on the store page, legal and the runner; a second re-freeze if the owner changes the scale after S3; any kernel fix a sitting surfaces below VaelenRun (headless within a day, but each costs a retake); the freedom rule and the colony kernel rule if the playtest demands them before 1.0. UNKNOWABLE UNTIL THE SITTINGS: whether the 11 walk files compile and PMC collision holds CharacterMovement; whether D3D11 already has Lumen off (then the A/B shows nothing and the 28 ms is elsewhere); whether the monolithic target links; whether the pak holds the engine content; whether a Shipping exe replays to the CI's digests; the eye-level frame time with 1 M vertices and 9,000 instances; the fun; whether a stranger pays.

=== DISSENT
Where the three disagreed and how I decided, checked against the repository rather than the proposals' descriptions of it. (1) LIVELY IN THE SHIPPED HOST - three answers: proposal 1 a new LivelyFrom option run only from the day play begins, measured against a written prediction; proposal 2 off until a post-S4 soak; proposal 3 fully on, citing 12.08's 2m05s under clang-release. Decided for proposal 1: the 2m05s (ROADMAP:3043) is the PLAY of 14400 days at the gate, while Begin WITH Lively over the pre-play years is 20.3 minutes, 3.3 GB RSS and a 2.37 GB container at 128 (ROADMAP:5221-5230) and 61 s/408 MiB even at 60+30 (:5273) - memory does not shrink in release, so proposal 3's cell is unshippable whatever the build; proposal 2's 'off' forfeits the only machinery that makes the world react to a name; LivelyFrom keeps both, at the cost of a HOST byte and a prediction that may fail, in which case proposal 2's answer is the fallback. (2) THE PREMISE - all three wrote 'set Colony=true and WantBound=1' as a two-line host flip; proposal 2 alone said 'if BeginEnslaved returns 0 the seed question goes to the owner'. Measured here with the prebuilt Atlas at HEAD: the colony wiring offers NOBODY bound at 128/300+120 (with and without --stream), while the host's own wiring with WantBound=1 offers Esvuhurdu bound to Arorde. So the bound start ships now at zero cost, the colony is a timeboxed kernel question, LivelyFrom does not rescue it, and proposal 1's acceptance 'a stand-in written with --colony' is impossible today because RunStand ignores the flag (Main.cpp:3044) - fixed as part of P-01. Proposal 3's Colony row on the page is deferred with the colony itself. (3) 'FREEDOM HAS A PRICE' (proposal 3's 19.14) - the one item that would give the premise an arc; also a kernel gameplay rule with a re-freeze on a premise cell that does not exist at 128 yet. Deferred past the first sale as the named first post-sale kernel task; the stranger playtest decides. Proposal 3 is right that without it the page reads 'worked, 4 hours of it' - I verified that page here - but a re-freeze before the first package has ever been made is the wrong order. (4) RENDERER CONFIG - proposal 1 commits it in the counted commit predicted from ROADMAP figures; proposal 2 measures by console A/B at S1-close first (and notes the RHI is D3D11, on which Lumen may already be off); proposal 3 commits it before S3 with a QWERTY table. Decided: proposal 2's measurement at S1-close (free), then proposal 1/3's commit in step 6 with the ADR Phase 13 owed. (5) THE SCALE - proposal 1 moves to 100 m tiles before S3 for pace; 2 and 3 keep 250 m. Decided: nobody has walked a region yet; S2 is the first datum; the decision folds into the one counted commit if it moves. (6) THE FRONT END - proposal 3 a kernel-side Menu leaf digested like Panel.h; proposal 1 a Canvas page in VaelenUI; proposal 2 a minimal start/settings screen. Decided: the host page (no kernel module change, no new pin, the same parse job sees it). (7) BEGIN OFF THE GAME THREAD - proposal 1 yes (Async thread), proposal 3 a loading row, proposal 2 silent. Decided: blocking behind a drawn page; a thread is a defect class nothing headless sees, for a wait inferred at 1-2 s in Development and measured at S4. (8) PACKAGING - proposal 1 lives S4 in the first Development package; proposal 2 a separate packaging sitting after S4 with a committed project map and LFS; proposal 3 a Shipping compile appended to S2 and a Shipping package at S6 on a second machine. Decided: a Development GAME-TARGET compile appended to S2 (report-only, the monolithic link is the unknown, not Shipping's defines yet); UAT Development opens S4 with the editor as fallback so the month on foot is never lost; Shipping at S5 on a second machine with the replay as acceptance; no committed map until a package proves Entry fails; windows-msvc-release in CI first (proposal 3). (9) CONTENT - proposal 2 two capped sittings at the end; proposal 3 one sitting before shipping; proposal 1 none, the store page saying so. Decided: optional, one capped sitting, only after the upload path works and only if the strangers name the look; the Development package goes to them in shapes. (10) THE SELF-HOSTED UBT RUNNER - proposal 3 only. Included as parallel and optional: the highest-leverage process change given that the chair is the critical path, but nothing waits on it. (11) THE SOAK - proposal 2's 20.01 at 3 years; proposal 3 a 10-year budget; proposal 1 extrapolates from S4's one figure. Decided: proposal 2's 3-year print-only Run.Soak before the sale (1 d, and it decides whether a size refusal is needed). (12) QWERTY - proposal 2 defers (W is Work), proposal 3 adds a table and toggle, proposal 1 binds the arrows. Decided: a second PanelKeys table with Work moved, a title-page toggle, no pin moves - 19.10 built exactly this mechanism. (13) AUTOSAVE - proposal 2 every N day turns, proposal 3 every turn, proposal 1 F5 only. Decided: every 10 day turns plus F5; every turn is a ~150 ms hitch on Space at 22 MB and grows with the log. (14) S1 CLOSE - unanimous (no rebuild, steps 2-6); proposal 2's A/B added. (15) COUNTS the proposals got wrong and I corrected from the tree: HEAD is 7 commits past 9d1f81b (not 8 or 9); there are 5 engine modules, not 3; 109 add_test entries under Tests/ (the '248 CTest entries' is the CI's per-leg count); Vaelen.Target.cs already lists VaelenPresentation (19.02 fixed what the plan had flagged).


## Proposal 1

### Summary

STATE (read at HEAD 5c985a0 = 19.09b, not the 9d1f81b the task named; 8 commits newer). Kernel: 14 headless modules, ~57 K lines, 248 CTest entries, 18 frozen gates (run_gates 67 min), CI on 10 legs green through run 309 (one self-test fixed in 19.06). Phases 00-15, 17, 18 closed; 16 built but its gate open (v4 migration half + 16.14 sitting); 19 headless half DONE (19.01, 19.02, 19.04, 19.05, 19.07, 19.08, 19.09/19.09b, 19.10), 19.03 = sitting S1 COMPILED BUT NOT CLOSED (ROADMAP:8193 says 19.03b compiled on the owner's machine 2026-09-25, yet Tests/Run/Sessions/ holds only the 09-16 pair, Tools/engine_builds.txt has no `s1` row, and no Session.P19S1 CTest entry exists), 19.06 = S2 WRITTEN and parsed (24 TUs, 94 shim beliefs), 19.11/19.12 not started. Engine side: 1,861 lines in 41 files under Source/{Vaelen,VaelenPresentation,VaelenGame,VaelenUI,VaelenWalk} changed since the last complete UBT build b0921 (867a129); 24 files UNVERIFIED (engine); 112 shim beliefs; ProceduralMeshComponent, Enhanced Input runtime contexts, C++ sky, FKey(FName) never compiled by UBT. WHAT A PLAYER GETS TODAY IN THE ENGINE: a console command (`Vaelen.Play 128 120`), a Canvas text page of one life (8 verbs on keys, Space turns the day), a Phase 13 map from above (a different world instance), save/load by console only. No title screen, no menu, no loading screen, no audio, no assets in git (Content/.gitkeep), no packaged build ever made, no Shipping config (and Shipping compiles the console out, so every Vaelen.* command vanishes), no Steam. THE PREMISE IS NOT IN THE ENGINE WORLD: VaelenWorldSubsystem::Begin (VaelenWorldSubsystem.cpp:136-160) sets Play+Stream only with WantBound=0 - no Colony (Phase 11), no Lively (Phase 12: NPC acts, hearsay, fame, judgement that binds/frees on a name). The kernel has all of it; the host does not ask. MEASURED HERE (Debug Atlas at HEAD, this container): 128/300+120 new game in the engine's wiring 36.6 s, peak RSS 59 MiB; with --colony 41.3 s, 59 MiB, `pits 1` in the layout; the page a player reads after 30 days is `Dokdahum ate, 1 hour of it` x16 (Tests/Run/Streams climate stand-in). Lively+Colony at the same cell, measured in Phase 16 (ROADMAP:5218-5230): Begin 20.3 min, RSS 3.3 GB, save 2.37 GB (22 M events x 112 B) - unshippable as wired; the cost is Phase 12's 200-per-mille daily acts over the 120 pre-play years, not the play. CADENCE: owner sittings on 09-07, 09-10, 09-14, 09-16, 09-21, 09-25 = one per 3 days, each new engine module has cost ~3 defects nothing headless could see (14.08); headless ~20 commits/day, Phase 19's eight headless tasks landed in one day. THESIS: the owner is the critical path, not the code. The shortest credible path is to finish Phase 19's sittings (merged to three), and, headless, in parallel, wire the premise into the host (colony, bound start, Phase 12 only from the day play begins) and build the missing product layer (front end, loading, save keys, cook/package config) so that EVERY remaining sitting also carries a piece of the product; then a packaged Win64 Development build on Steam Early Access, priced and described as what it is: one life in a procedurally generated living world (winters, bondage, roads, hearsay, judgement, death then another life), walked in 3D in abstract shapes, keyboard only, no audio. What is NOT sellable and must not be attempted first: the text page alone, an empty cube-land with no people reacting, or anything driven from the console. Everything in Phases 20-22 as planned is deferred; only the eight ship items named below are pulled forward.

### Order

- 1. CLOSE S1 (19.03) WITH WHAT ALREADY COMPILED. The owner's 19.03b build succeeded on 2026-09-25 (ROADMAP:8193), so step 1 is known green; ask for steps 2-6 of ENGINE_HANDOFF 'PHASE 19 - sitting S1' on that same checkout (about one hour at the console: `Vaelen.View 128 120`, the 16.14 save/close/reopen/load steps, `Vaelen.Play 128 120 1` + 3 days + `Vaelen.Stream.Write` + the Weather-row screenshot, `stat unit` twice, `Vaelen.Load host24-16`), with `git rev-parse HEAD` first. Headless when it lands: Tests/Run/Sessions/s1-<date>.log + s1.session, a `Session.P19S1` entry in Tests/Run/CMakeLists.txt (none exists yet), `s1` RECORDED in Tools/engine_builds.txt, check_engine_status.py moves the 7 files. ACCEPT: `ctest -R '^Session\.P19S1$'` green; the log carries `frame ec18241b89c3d246, ground 8f7f4948f49b6e86`; the host24 check line ends `--no-climate`; `Run.Gate.Saved` adopts the engine's container to the printed state. WHY: it closes Phase 16's 16.14 and Phase 18's engine line at once, it is the frame-time baseline every later clause is measured against, and it costs no new build.
- 2. P-01 THE PREMISE IN THE HOST (headless, half a day, lands before S3's commit, not S2's). VaelenWorldSubsystem::Begin: `Asked.Colony = true`, `Rules.WantBound = 1` with the documented fallback to 0 when Start.h's search finds nobody bound; HeadlessCheck gains ` --colony`; `Vaelen.Walk` stands the walker on the colony's region (NetView::ColonyIn) instead of the centroid of whatever region; default size 128. ACCEPT: a new stand-in stream written with `--colony --stand --want-bound 1` pinned as `Replay.Colony` (four digests); `Atlas.SceneLayout128` with --colony prints `pits 1`; the played page's Self row says `bound to <holder>`; census diff 0 removed (WORLD/stream kind added); measured here +4.7 s and +0 MiB at 128 Debug. WHY: the game's one-sentence pitch is 'you start enslaved in a mining colony' and the engine world has neither the colony nor a bound start; two lines fix it, and doing it before the first product sitting means the first walk anyone sees is the product's walk.
- 3. S2 (19.06) AS WRITTEN IN ENGINE_HANDOFF 'PHASE 19 - sitting S2', pinned to commit 19d6a6a: build, launch `/Engine/Maps/Entry?game=/Script/VaelenWalk.VaelenWalkGameMode -game`, `Vaelen.Walk 128 120`, `Vaelen.Probe 64` and `Vaelen.Probe 64 50`, Z/Q/S/D once each, the steepest slope, Space x3, `stat unit`, two screenshots, the `Vaelen.View` control. ACCEPT: `Session.P19S2`: terrain line byte-equal to `VaelenAtlas --size 128 --prehistory 300 --years 120 --scene-terrain R`, climate line byte-equal, probe <= 1.0 cm with 0 misses and the forced bias printing 5.0, four move lines (0,1)(-1,0)(0,-1)(1,0). Budget 2 sittings: 11 new engine files, PMC collision under CharacterMovement, runtime mapping contexts and FKey(FName) are all beliefs. WHY: every API the rest of the phase and the product stand on runs once here; if one is refused only the engine side is redesigned (ADR-0157 names the fallbacks: BindAxisKey, VertexColorMaterial).
- 4. P-02 LIVELY FROM THE DAY PLAY BEGINS (headless, 2-3 days, while S2 waits). Kernel: `Run::Options::LivelyFrom` (a year; 0 = as today) so Phase 12's Living, Repute, Fame and Judgement systems are DECLARED at wiring but RUN only from year >= LivelyFrom; Begin sets it to PreHistory+Years for the host; a HOST byte in the 18.02 pattern (25 -> 26 bytes, the reading rule, refused by name on adopt); Atlas `--lively-from N`; the host asks for it. Predict in writing first, then measure: Begin at 128/300+120 with LivelyFrom = play <= 1.2x the Play-only Begin (~44 s Debug here) and RSS <= 200 MiB (the 20-minute/3.3 GB cell was 311 M pre-play acts at 200 per mille a day; from play it is 1/480 of that); 90 played days at 128 produce >= 1 hearsay reaching the played person, >= 1 name carried down a road, judgement counts > 0. ACCEPT: `Run.LivelyFromPlay` with those bounds, `Run.Checkpoint` gains TheLivelyFromIsInTheHostSection (a 25-byte HOST still reads), `Replay.Colony` re-pinned once with it, census 0 removed. If the measurement fails, ship Play+Colony only and say so on the store page. WHY: Phase 12 is the only machinery that makes the world REACT to the player (opinions travel, a name gets you bound or freed); without it the walker is in scenery and a stranger has nothing to pay for; as wired today it costs 20 minutes and 3.3 GB per new game.
- 5. THE ONE COUNTED WORLD-MOVING COMMIT BEFORE S3 (headless, 1 day, the 18.10 form): settle owner Q1/Q2/Q3/Q9 in one commit whose census footprint is predicted first. Q1 scale for PACE: `SceneScale{CmPerTile 10000, ReliefPerMille 100}` keeps the 1/1000 ratio (0 steep triangles) and makes a median region ~0.85 km, ~3 minutes to cross instead of 7; Q2 keys: `Keys_ = WalkKeys` (Speak on F) and the arrows bound too; Q3: crossing by M at the fence (as built in 19.07); Q9: `GlobalDefaultGameMode=/Script/VaelenWalk.VaelenWalkGameMode`. Same commit: DefaultEngine.ini `r.DynamicGlobalIlluminationMethod=0`, `r.ReflectionMethod=0`, Nanite and VT off (the T400's 28 ms template-map baseline is Lumen + clouds with nothing of ours drawn, ROADMAP:4641). ACCEPT: `Atlas.SceneTerrain128`, `Atlas.SceneLayout128`, `Atlas.SceneSky128`, `Replay.Climate`'s three scene lines and `View.Panel`'s walk page re-pinned ONCE, predicted in the message; `Scene.Terrain` steep still 0 at 128 and 256; `frozen_census.py --diff` shows exactly the predicted WORLD/terrain, layout, sky, panel rows and 0 GEN/HASH. WHY: each of these moves pins; three separate re-freezes in a week is how ADR-0095 happened; and the scale must be right BEFORE S3 records the first replayed scene.
- 6. S3 (19.11) + P-01 + step 5 SEEN: AVaelenScenery draws the layout as ISMs from /Engine/BasicShapes (houses cube+cone with collision, figures cylinder+sphere, the square, roads, the colony pit), AVaelenLand repaints snow/grass on OnViewsTaken, WalkKeys on the controller, M at the fence -> CrossingOf, Speak/Give/Take -> AimAt with Tab fallback, one `LogVaelenWalk: day D tile T region R life L looked K` line a day, `Vaelen.Scene`. ACCEPT: `Session.P19S3`: `VaelenAtlas --replay <S3 stream> --panel --want-bound 1 --colony --lively-from <year> --stream --keys TWREMFGK --scene` prints the Scene, Climate and Play lines byte-identical as files, every LogVaelenWalk line has region = life = looked, four `blocked` lines, ISM instance counts == the layout line's counts, `stat unit` at eye level in the colony with Lumen off recorded as the clause (n) figure. WHY: this is the first sitting where what the owner sees is the played world (not the Phase 13 actor's separate instance) with people in it; and it is the frame-time reading that decides Q4.
- 7. P-03 THE FRONT END, ASSET-FREE (headless engine code parsed against the shim, 2 days, lands for S4). VaelenUI: a Canvas text title page in the 14.09 style (no UMG, no Blueprint): [N]ew life (128/120), [C]ontinue (the newest usable name from `Saves()`), [Q]uit; Esc in play = a pause page with [S]ave / [L]oad / [Q]uit; F5 = Save("auto") and F8 = Load("auto"). VaelenGame Private: Begin() moved off the game thread (`Async(EAsyncExecution::Thread, ...)` owning the Aelvor until Begun; the kernel is engine-free; the UI fence is untouched because VaelenUI/VaelenWalk never see a thread) behind an 'AELVOR is being made' page; the `.previous` rename-aside in FVaelenCheckpointStore and the kernel store (Phase 16's open promise: a write cannot destroy the last good NAME, ROADMAP 'Found by the 19.10 audit'). Every path key-driven, because Shipping has no console. ACCEPT headless: parse clean, `Kernel.UiFence`, `Kernel.EngineStatus` (new files UNVERIFIED), `Run.SaveAside` (a write over an existing name keeps `<name>.previous` hidden from listings; Read of a missing name restores it; CannotWrite leaves both). Owner acceptance at S4. WHY: a product starts, continues and quits without a console; today none of those exist.
- 8. P-04 PACKAGING, CONFIG AND A COOK CHECK (headless, 1 day, lands for S4). DefaultGame.ini: `+DirectoriesToAlwaysCook=(Path="/Engine/BasicShapes")`, `+MapsToCook=(FilePath="/Engine/Maps/Entry")`, ProjectVersion 0.1.0, CompanyName; DefaultEngine.ini GameDefaultMap stays Entry with the walk mode as GlobalDefaultGameMode; remove the `ConstructorHelpers::FObjectFinder` of the never-committed `/Game/M_VaelenTile` in VaelenLand.cpp:38 and VaelenViewActor.cpp:331 (an error in every packaged start) - vertex colours only; `Tools/check_cook.py` (`Kernel.Cook`): every `/Engine/...` or `/Game/...` path a Source/ file names must be under a cooked directory or map (known answer at HEAD: BasicShapes/Cube, BasicShapeMaterial, WorldGridMaterial, EngineDebugMaterials/VertexColorMaterial, DefaultMaterial, Maps/Entry, and M_VaelenTile refused). ENGINE_HANDOFF gains the UAT line: `RunUAT BuildCookRun -project=<path>\Vaelen.uproject -platform=Win64 -clientconfig=Development -build -cook -stage -pak -archive -archivedirectory=<dir>` (Development FIRST: the console stays for diagnosis; Shipping is step 10). ACCEPT: `Kernel.Cook` green with its known answer; S4 launches the archived Vaelen.exe. WHY: nothing has ever been packaged; assets loaded by path at runtime are not in a pak unless listed, and this is the class of failure that costs a whole sitting to discover on the owner's machine.
- 9. S4 (19.12 + P-02 + P-03 + P-04 SEEN) IN THE PACKAGED BUILD, NOT THE EDITOR: run UAT once (bring back its result and the first error if any), launch the archived exe, [N]ew life, walk >= 30 days: every verb, >= 2 crossings by M, >= 1 refused crossing, >= 1 Speak aimed at a figure, into the first frosts, F5, [Q]uit, relaunch, [C]ontinue, 5 more days, `Vaelen.Stream.Write` (Development keeps the console), a screenshot whose last page row is the panel digest, `stat unit`, and the container size and the process RSS after 30 days. ACCEPT: `Replay.OnFoot` and `Run.Gate.OnFoot` as gates 19 and 20 (`N of N answered identically, D of D days`, P == 0, C >= 2, A >= 1), `Session.P19S4`, `Run.Gate.Saved` over the packaged build's container (closes 16.14 for good), clause (n) against S3's reading; `Tests/Run/Streams/aelvor128-onfoot-<date>.stream` committed. Phase 19 closes here by its own fourteen clauses, STATUS moved by the checker. WHY: the first sale needs one life lived by a person in the exact binary a stranger will run; the replay proves the binary is the world the CI holds.
- 10. P-05 THE FIRST SALE (owner's hours, no code): Steamworks partner account and the app fee; a store page that says exactly what the build is (abstract shapes, keyboard only, English, no audio, saves may be refused by name across updates); depot upload of the S4 Development package; price low; Early Access. Before upload, hand the S4 package to 2-3 strangers for one evening each and ask one question - would you pay for this - and write the answers into ROADMAP. Then P-06, one more sitting: the same UAT line with `-clientconfig=Shipping` (asserts off - the noasserts CI presets already cover the kernel; the console gone - the front end must suffice), replayed once more to the same digests, uploaded as the second build. ACCEPT: a stranger downloads, starts a life, saves, continues; the stream they send back replays. WHY: everything before this is engineering; this is the only test of 'would a stranger pay'.

### Cuts

- Phase 20 STRESS TEST as planned (256 maps, 500-year runs, log tiering over 49 Log().All() sites): ship 128 only (59 MiB RSS, ~40 s Debug Begin here); the one figure needed - container size and RSS after a played month in the shipped cell - is taken at S4 and extrapolated; log tiering waits, because it moves ~178 frozen literals.
- Phase 21 MODDING entirely: no stable ids, no data-driven definitions, no mod loader before a sale.
- Phase 22 POLISH except the four ship items pulled forward (front end, loading page, save keys, packaging): no balance pass, no settings menu (Alt+Enter and engine defaults), no save browser/thumbnails, no localisation (the page is composed and digested kernel-side; a French page moves every panel pin and is its own post-sale phase with a re-freeze), no audio (say so on the store page), no achievements/Steam SDK code (the overlay needs none).
- Phase 16's migration half (the v4 format bump, Save.CorpusMigrates and the forge): ship v3 containers; 16.08 already refuses an incompatible save BY NAME and never half-loads, which is the guarantee an Early Access needs; a migration chain is post-sale.
- Lively over the 120 pre-play years: never (20 minutes and 3.3 GB per new game). Either LivelyFrom = play (step 4) or Lively off; the decision is a measurement, not a wish.
- Everything ROADMAP section 26 already lists as NOT in Phase 19, kept out: Infrastructure wired into the played world (Q7, its own re-freeze + HOST byte), a kernel position for people below the region, travel within a day or an automatic day (Q8), skeletal characters/Mannequin/animation blueprints/Landscape/Nanite/World Partition (Q5, Q6), NPCs walking, a water leaf, saving the walker's position, a fifth wiring, new verbs/combat/inventory, re-recording the 09-16 and 09-21 months, the Atlas --walk3d stand-in.
- The Phase 13 map actor (AVaelenViewActor / Vaelen.View, its own second world instance, 72 K instances, 30 ms at ground level on the T400): stays compiled for S1's control and the digests, never spawned by the front end, not in the shipped loop.
- 256-tile worlds in the shipped build, Lumen, Nanite, virtual textures, the /Game/M_VaelenTile asset that lives on one disk, UMG/Slate/Blueprints/Content of any kind (ADR-0157 is asset-free and that is what makes the CI able to see the engine code at all).
- Merging sittings rather than adding them: S3 and S4 carry the product checks (P-01 to P-04) instead of getting sittings of their own; the plan's budget of six stays the ceiling. ADR-0129 and ADR-0131 (roads never abandoned, goods per road unnamed) stay open.

### Risks

- Engine defects nothing headless can see: 24 engine files never compiled by UBT, 112 shim beliefs (94 for the walk), ProceduralMeshComponent complex collision under CharacterMovement, runtime Enhanced Input mapping contexts, FKey(FName), a C++ sky in a lightless map, DLL-build-only errors of the C2487 class (Kernel.DllApi sees one class of them and says it does not see the rest). History: 3 defects per new module at 14.08, and S1's first attempt failed at step 1. => Sittings pinned to a commit found by subject, rev-parse first, first error only, REPORT not repair; S2 runs every belief once before anything is built on it; ADR-0157's fallbacks ready (BindAxisKey, VertexColorMaterial); a budget of 6 sittings with 5-9 planned here; nothing engine-side lands between two sittings except what the next sitting's commit names.
- The fun is unproven: after 30 played days the page reads 'Dokdahum ate, 1 hour of it' sixteen times; in the engine's current wiring nobody reacts to the player (no Lively, no colony, not bound). A stranger pays for consequence, not for a simulation that runs without them. => Step 2 (bound start in the colony) and step 4 (Phase 12 from the day play begins, with a measured Begin cost) put hearsay, fame and judgement on the page; the owner lives 30+ days in the S4 package and writes a verdict; 2-3 strangers play the Development package before upload; if step 4's measurement fails the store page and price say 'sandbox', not 'RPG'.
- Frame time on the owner's T400 (the min-spec by default): 30.4 ms GPU-bound at ground level over the 13.09 scene, 28.15 ms for the template map with the HUD alone; the walk uploads 1,065,024 vertices / 2,097,152 triangles at 128 plus ISMs for 7,039 houses and 1,960 figures at day 31. => Lumen/Nanite/VT off in one config commit before S3; far chunks at stride 8 as built; S2 and S3 each bring `stat unit` at eye level; clause (n) gets a measured target (propose <= 33 ms on the T400 as the floor, since Steam min-spec is above it); if GPU-bound remains, cull figures beyond the played region and its ring (a rule in Layout/AVaelenScenery, no kernel change).
- Packaging has never been done: assets loaded by path at runtime (/Engine/BasicShapes, engine materials, /Engine/Maps/Entry) are not cooked unless listed; the FObjectFinder of the absent /Game/M_VaelenTile errors on every start; Shipping compiles the console out and every command the game is driven by today is a console command; DO_CHECK is 0 in Shipping. => Step 8's check_cook.py with a known answer at HEAD; Development config first, Shipping second; step 7 makes every path a key; the kernel's noasserts CI presets already prove it runs with checks off; the UAT line written into ENGINE_HANDOFF with what to bring back.
- Save size and memory over a long life: the event log is 112 B/event and three quarters of every image, Phase 16 truncates nothing, and with Phase 12 on the log can grow to gigabytes (2.37 GB measured at the Lively cell); a 95-second rollback copy on every load doubles peak memory. => LivelyFrom = play keeps the pre-play log at the Play-only size (~22 MB at 128); S4 measures the container and RSS after 30 days and the per-year slope is written down; before the sale a hard refusal by name above a stated size (the store already refuses by name) rather than truncation; log tiering stays Phase 20.
- Save compatibility across Early Access updates: v3 containers, no migration chain, and any type added, removed or resized (the re-freeze census counts these) refuses every existing save. => 16.08's refusal by name means a save is never half-applied; the EA notes say saves may be refused between builds; VAELEN_SAVE_FORMAT_VERSION discipline and the census stay; the migration chain is the first post-sale kernel task.
- The owner is the critical path: one sitting per ~3 days, each a build-report-fix loop, and the roadmap's own history shows an unmeasured clause (14.10's fps) or a missing verb (15.10's TakeSomebodyElse) can cost a week. => Each sitting's section in ENGINE_HANDOFF lists exact commands and the lines to bring back; S3 and S4 carry the product checks so no sitting is 'only' verification; S1 closes on the build that already compiled; the reader (check_session.py) refuses a log without its rev-parse so no sitting is spent on the wrong commit.
- The discipline machinery slows product decisions: the scale, the keys, the colony in the host and LivelyFrom each move frozen pins, the gates take 67 minutes, and three re-freezes in a week is the ADR-0095 pattern. => Step 5 batches every world-moving decision into one counted commit in the 18.10 form (prediction table first, census after); the gates run at that head and at S4's, the touched suites and the census at every commit, as section 26 already says.
- S1's evidence is incomplete: the 19.03b build compiled on 2026-09-25 but no log came back, so Phase 16's and Phase 18's engine lines are still unclosed and the frame baseline for clause (n) does not exist. => The first ask of the owner is steps 2-6 on the 19.03b checkout (one hour, no new build), not a new sitting; the session entry and ledger row are written the day it lands.
- Commercial and legal debt nothing in the repo covers: CompanyName empty, ProjectVersion 0.0.1, no EULA or privacy text, the name VAELEN unchecked, the Steam fee and review time. => Step 10 is an owner checklist with dates, started in parallel with S2 (Steam review and store assets take about a week and need nothing from the code).

### Estimate

OWNER SITTINGS (each 1-3 hours at the machine, plus the report): S1-close 1 (the build already compiled; console steps only); S2 1-2 (11 new engine files, 94 beliefs, PMC + Enhanced Input first run); S3 1-2 (scenery, walk keys, colony start, Lumen off, the frame reading); S4 1-2 (first UAT package, front end, a 30-day life, save/continue, the on-foot gate); Shipping package + Steam upload 1. TOTAL 5-9 sittings, median 7 - inside the plan's budget of 6 for Phase 19 proper plus 1-3 for shipping. HEADLESS: P-01 0.5 d; P-02 LivelyFrom 2-3 d (kernel option, HOST byte, measurement, re-pin); step 5's counted re-freeze 1 d; Session readers/gates for S2-S4 0.5 d each; P-03 front end 2 d; P-04 packaging + check_cook 1 d; fix loops after each sitting 0.5-1 d each (5 sittings); docs/ADRs/STATUS 1 d; TOTAL 10-13 working days = 2-2.5 headless weeks, none of it on the critical path. CALENDAR: at the observed cadence (6 sittings in 18 days) with the owner sitting twice a week, first Steam Early Access upload 4-6 weeks from 2026-09-25 (early to mid November 2026); at one sitting a week, 8-9 weeks. ASSUMPTIONS: the owner sits >= 2x/week; S2 needs <= 2 sittings and no UE API contradiction forces a redesign beyond ADR-0157's named fallbacks; P-02's prediction holds (Begin with LivelyFrom = play <= 1.2x Play-only, RSS <= 200 MiB; if not, ship Play+Colony only, which saves 2 headless days and changes nothing else); the T400 reads <= 33 ms at eye level with Lumen off (else one more culling task, ~1 d + the S3 reading); Steam review ~1 week and the store page done in parallel from S2 on; no audio, animation, localisation or settings before the sale; the stranger playtest (2-3 people, one evening each, in the S4 week) adds up to one week and is the only evidence for 'would pay'. NOT COUNTED: the owner's own hours on the store page and legal; a second re-freeze if the owner changes the scale after S3; any kernel fix the sittings surface inside Source/VaelenRun or below (report-not-repair puts it headless within a day, but each costs a retake).

## Proposal 2

### Summary

STATE (read at HEAD 5c985a0, seven commits past the 9d1f81b the task names): kernel Phases 00-18 closed headless, 248 CTest entries, 18 frozen gates (67 min), integer-only scene module (19.05/07/08/09), walk code (19.06) written and parsed but never compiled, S1 retake of 19.03b compiled on 2026-09-25 but its log is not committed (no `s1` row in Tools/engine_builds.txt, Tests/Run/Sessions holds only s0916). Engine builds have happened six times in 19 days, each finding 1-3 defects nothing headless can see. What a player gets today: a console command (`Vaelen.Play`), a Canvas text page (VaelenHUD.cpp: small font, one DrawText per row) with eight verbs, Space to turn the day, a chronicle that reads "Dikrisso worked, 4 hours of it." (verified by replaying Tests/Run/Streams/aelvor128-climate-stand-2026-09-25.stream with the prebuilt Atlas), no menu, no save UI, no audio, no assets (Content/ holds .gitkeep), a map from engine template `/Engine/Maps/Entry`, and 28-30 ms GPU on the owner's T400 before any scene is drawn (DefaultEngine.ini enables Lumen GI/reflections and Nanite). THE DECISIVE FINDING: `UVaelenWorldSubsystem::Begin` (Source/VaelenGame/Private/VaelenWorldSubsystem.cpp:141-155) wires `Asked.Play = true` only and `Rules.WantBound = 0` - the engine's world has NO mining colony (Phase 11), NO living NPCs/repute/fame/judgement (Phase 12) and the player is NOT enslaved. The game's premise ("enslaved in a huge autonomous mining colony", "history belongs to no one") is not in the world a player would buy; the Atlas already supports `--colony --lively` and the container's HOST section already carries those bytes, so this is a host flip, not new machinery. Second finding: Phase 12 closed with "the world gives the reputation machinery nothing to carry" because nobody moves, and bondage ends only by yearly dice (Bondage.h:67-68, ManumissionPerMille 25, FlightPerMille 8) - a bound player has no act that changes their own fate. Third: the `Vaelen` Game target (monolithic, `*_API` empty) and any Shipping configuration have never been linked (STATUS.md "Unverified"); `windows-msvc-release` is built by no CI job. RECOMMENDED PATH: keep Phase 19's four sittings as the spine (the engine contract must run once before anything is built on it), but interleave three headless tasks that decide the first ten minutes - the premise flipped on in the host with the colony row on the page, one gameplay rule making freedom a function of the player's acts (feeding Phase 12's idle fame/judgement), and a renderer config that gives the T400 back ~20 ms - then a menu/save page composed kernel-side like the panel, a Shipping package sitting, one asset sitting (Mannequin + three sounds + one map under LFS), and a min-spec playtest. Cut modding, the 256 stress test, save migration, infrastructure wiring, animated NPCs, controller, localisation. Use the Atlas (`--replay --panel`, `--stand`, `--walk`) as the zero-cost design instrument for the opening before any sitting is spent on it, and turn the owner's PC into a self-hosted UBT compile runner so a compile error no longer costs a sitting.

### Order

- 1. CLOSE S1 (owner, already scheduled, 19.03): commit the retake's Saved/Logs/Vaelen.log as Tests/Run/Sessions/s1-2026-09-25.log with `git rev-parse` first, add the RECORDED row `s1` to Tools/engine_builds.txt, let Tools/check_engine_status.py move the seven files to VALIDATED s1. WHY: it is the only thing that turns 410 parsed lines (16.14 save/load, 18.10 climate wiring, the ADR-0135 pair ec18241b89c3d246/8f7f4948f49b6e86) into built ones, and every later sitting is refused without a `head`. ACCEPTANCE: `ctest -R '^Session\.P19S1$|^Kernel\.EngineStatus$'` green; step 6's host24-16 load prints `--no-climate`.
- 2. THE PREMISE IN THE HOST (headless, in parallel with 1, new task 19.13): in Source/VaelenGame/Private/VaelenWorldSubsystem.cpp `Begin` set `Asked.Colony = true`, `Asked.Lively = true`, `Asked.Stream = true` (the daily cadence, 15.10) and `Rules.WantBound = 1`; `HeadlessCheck` already prints `--colony --lively --stream --climate`. Add a Colony row to the page after Weather (Source/VaelenView/Public/Vaelen/View/Panel.h RowKind appended after Weather so ordinals hold; Net.h:54 `ColonyView{Region, Hands, Lifted}` is already a leaf) and keep the Self row's `bound to <holder> since year N` (Panel.cpp:391). Record a stand-in month with `VaelenAtlas --stand ... --colony --lively --stream --want-bound 1` as Tests/Run/Streams/aelvor128-premise-<date>.stream and pin `Replay.Premise` in Tests/Run/CMakeLists.txt through ReplayPlayed.cmake. WHY: minute one must show a bound person in a mine among people who act; every digest pinned today belongs to other wirings (Replay.Played/Lived/Walked pass their own flags), so the flip moves nothing frozen; 12.08's gate ran 14400 played days in 2m05s under clang-release, so Lively is affordable. ACCEPTANCE: the replayed page's Self row reads `bound to ...`, the Colony row is present, the four digests replay identically; the `LogVaelenPlay: day D year Y: ... X ms` line at 128 with Lively is <= 5 ms in release; census `--diff` shows 0 removed.
- 3. FREEDOM HAS A PRICE (headless, kernel, new task 19.14, switch-then-flip like 18.02/18.10): a bound played person's manumission and flight become functions of acts through the systems that own them - Give to the holder (Doings.h `give -> AddStock`) accumulates toward manumission (Society/Bondage.h ManumissionPerMille), a Move out of the holder's region while bound is flight, and the place judges the name that arrives by roads (VaelenGameplay Fame.h/Judgement.h, built in 12.05-12.06 and never fed because nobody moved). Behind `Run::Options::Freedom` (Aelvor.h:73-106) first, flipped with one re-freeze commit predicted by Tools/frozen_census.py, ADR proposed. The page's Self row says what would free them (`manumission at N given` / `flight risks judgement in R`). WHY: it is the only arc the premise offers, it uses no new system, and it turns 12.08's two-step why-chains into real consequences; without a goal the ten minutes are 'worked, 4 hours of it'. ACCEPTANCE: new `Run.Freedom` CTest: a scripted 200-day stream in which giving frees the player, replayed to the same four digests; a flight stream in which the player is re-bound by a judgement whose `WhyOf` chain is >= 3 steps (GameplayHistory); with the switch off every existing pin is byte-identical (Replay.Played, Run.Golden, Player/Colony/Gameplay gates); CONTROL: a free player's page has no price row.
- 4. S2 (owner, 19.06 as written in ENGINE_HANDOFF 'sitting S2') PLUS one compile-only step appended after step 10: `Build.bat Vaelen Win64 Shipping` (the Game target, Source/Vaelen.Target.cs, monolithic and with every `VAELEN_ASSERTS_ENABLED=0` / `VAELEN_LOG_COMPILED_MIN_LEVEL=2` branch of the Build.cs files) - report the first error only. WHY: the walk rests on six engine APIs never run (PMC collision under CMC, runtime mapping context, ZQSD on AZERTY, C++ sky, VertexColorMaterial, FKey(FName)); the sale rests on a target never linked, and finding both causes in one sitting halves the owner's calendar. ACCEPTANCE: `Session.P19S2` green (terrain and climate lines byte-equal to `--scene-terrain R`, probe <= 1 cm with 0 misses and the forced 5.0, four ZQSD lines); a UBT `Result:` line for the Shipping game target in the committed log.
- 5. FRAME BUDGET AND KEYS (headless, one commit, before S3): Config/DefaultEngine.ini `[/Script/Engine.RendererSettings]` r.DynamicGlobalIlluminationMethod=1 -> 0, r.ReflectionMethod=1 -> 0, r.Nanite.ProjectEnabled -> False (no Nanite mesh exists; Lumen software GI is the likeliest 28 ms on a 4 GB T400), plus a DefaultScalability.ini pinning sg.* for the min spec; a QWERTY table beside WalkKeys (Panel.h PanelKeys/ValidKeys) and a runtime layout toggle in Source/VaelenUI/Private/VaelenWalkController.cpp (the mapping context is already made at run time, ADR-0157). WHY: question 4's baseline (28.15 ms with nothing drawn) is a config defect, not a scene cost, and 30 fps at eye level is the one number every review quotes; a Steam audience is mostly WASD. ACCEPTANCE: measured at S3 - `stat unit` on the template map with the HUD <= 8 ms GPU on the T400; `View.Panel` pins the QWERTY page (one row differs); `Atlas.Keys128` shows `--keys` moves only the panel digest.
- 6. S3 (owner, 19.11 as written): scenery, both seasons, `stat unit` in the settlement with the new renderer config, four `blocked` lines. ACCEPTANCE: `Session.P19S3` green (Scene/Climate/Play lines byte-identical as files, region = life = looked); frame time at eye level <= 16.7 ms on the T400 (proposed answer to owner question 4: 60 fps on GTX 1060-class min spec, 30+ on the T400 as the floor).
- 7. NO CONSOLE BETWEEN THE PLAYER AND THE WORLD (headless, new task 19.15): a `Vaelen/View/Menu.h` leaf composed and digested like Panel.h (rows: New life / Continue / Load <from Saves()> / Keys AZERTY-QWERTY / Quit; a loading row while `Begin` generates - 128/120 is ~4 s release, 38.7 s in the gcc Debug Atlas), drawn by the same Canvas path in VaelenHUD.cpp; the game opens in the walker (owner question 9: `GameDefaultMap` and `GlobalDefaultGameMode` in DefaultEngine.ini pointing at VaelenWalkGameMode); Continue/Load go through the 16.14 store (`Save`, `Load`, `Saves`), with autosave at each day turn under a fixed name and the still-open `.previous` rename-aside closed (ROADMAP 'Found by the 19.10 audit'). WHY: a game that starts from a console command cannot be sold; the store exists (16.14) but only as commands. ACCEPTANCE: `View.Menu` pins the menu page; headless `Run.Autosave`: a life saved at day D by the store and adopted by a fresh host reaches the same state digest (16.14's `check it headless` line); on the owner's machine (S4) the walker is reached from the packaged exe with no console input, and a Continue after closing the game prints `loaded` with the saved state digest.
- 8. S4 (owner, 19.12 as written + step 7): a life on foot from the menu, 30+ days, every verb, 2 crossings, 1 refused, 1 aimed Speak, into winter; `Vaelen.Stream.Write`; then Quit, relaunch, Continue. ACCEPTANCE: `Replay.OnFoot`, `Run.Gate.OnFoot`, `Session.P19S4` green (P == 0, C >= 2, A >= 1); the Continue's `loaded` state digest equals the autosave's; gates 19 and 20 added to Tools/run_gates.sh.
- 9. A BODY AND A SOUND (headless prep, then owner S5; new task 19.16): decide owner question 5 as YES with Git LFS (.gitattributes for *.uasset/*.umap/*.wav); commit the UE Third Person Mannequin (SKM_Manny + its ABP, licensed for UE projects) and set it on `AVaelenWalker` by object path in Source/VaelenWalk/Private/VaelenWalker.cpp (idle/walk only; NPC figures stay basic shapes); three CC0 sounds (wind loop under AVaelenSky, a footstep on the CMC, a click on Press) via UAudioComponent set by path; one project map Content/Maps/Aelvor.umap holding nothing but the game mode; M_VaelenTile committed so the `VertexColorMaterial` fallback in VaelenLand.cpp is no longer the shipped look. WHY: a capsule with no sound is the single sentence every negative review would open with; this is one sitting and a few hundred MB under LFS. ACCEPTANCE: a fresh clone with LFS builds and runs; screenshot from the walker; `stat unit` not worse than S3 by more than 2 ms; `Kernel.UiFence` and `Kernel.ShimLedger` still green (paths are strings, no new API beliefs beyond USkeletalMeshComponent/UAudioComponent listed in Tools/shim_beliefs.txt).
- 10. SHIP IT ONCE (owner S6, new task 19.17): add `windows-msvc-release` (CMakePresets.json:150, built by no job) to .github/workflows/kernel-ci.yml first (the kernel is integer-only under R8, so release digests should hold - prove it before trusting a Shipping exe); then `RunUAT BuildCookRun` for `Vaelen Win64 Shipping` (Config/DefaultGame.ini ProjectPackagingSettings PPBC_Development -> Shipping, ProjectVersion 0.0.1 -> 0.1.0), run the exe on a SECOND machine at min spec for 30 minutes from a cold start, write the stream, upload to a private Steam branch. WHY: nothing in this repository has ever been packaged, cooked or run outside the editor. ACCEPTANCE: `Session.P19S6`: the packaged build's `Vaelen.Stream.Write` lines replay headlessly to the same four digests (the release-build determinism nobody has checked); menu in < 10 s, walker in < 10 s after New life, no crash in 30 min; the pak contains only the committed assets.
- 11. THE EA BUDGET AND THE NOTES (headless, Phase 20 cut down): a 10-year played life at 128 (3600 day turns, Lively + Colony + Climate) measured for day-turn ms, resident memory and container size, gated as bounds not digests; Docs/EA_NOTES.md stating what the first sale is (128 only, EN only, keyboard+mouse only, saves not migrated between versions, day on a key); ARCHITECTURE/ROADMAP/STATUS refreshed, ADR-0155..0159 applied plus the new ones (Freedom, LFS, self-hosted runner). ACCEPTANCE: `Run.Budget` green on linux-clang-release with the three bounds written before the first run; the notes match the store page word for word.
- 12. IN PARALLEL FROM DAY ONE (owner, once, ~1 h; headless writes the workflow): a self-hosted GitHub Actions runner on the owner's PC running `Build.bat` (UBT compile of VaelenEditor and Vaelen Shipping, no play) on every push to the branch, amending ADR-0134 ('the CI cannot see the module'). WHY: every sitting so far has spent its first hour on a compile error (C2487, the UHT destructor, LNK2019 on RegionGraphCache); with compile in CI the owner's sittings are spent looking and playing, which is the only thing the machine is needed for. ACCEPTANCE: a push with a planted `*_API` on a member fails the runner with the C2487 line in the Actions log while Kernel.DllApi also refuses it; a clean push reports `Result: Succeeded` for both targets.

### Cuts

- Phase 21 MODDING entirely: data-driven definitions and mod APIs sell to nobody before there is a game; stable ids already exist (ADR-0004).
- Phase 20 STRESS TEST as planned (256 maps, 500-year runs, the 182 MB save, log tiering over 49 `Log().All()` sites): reduce to the 128 EA budget of step 11; ship 128 only (a median region is ~2.1 km at 250 m tiles, 99 regions, 36k alive).
- Phase 16 clause (j), the migration chain to the v4 format: EA saves are versioned and refused when unknown (`DecodeStream`, the container header); say on the store page that saves may not carry across EA versions instead of building the chain.
- Owner question 7, wiring VaelenInfrastructure into the played world: its own re-freeze plus a HOST byte, and even wired it holds no house (Buildings.h:44-51); houses from families (19.08) are the shipped look.
- A water leaf, flowing rivers, lake spill levels; NPCs walking or animated; skeletal NPCs; Landscape, Nanite, World Partition - all named out of Phase 19 already, and none is in the first ten minutes.
- Owner question 8 (time passing while walking): keep the day on Space; it is what makes the walk a replayable stream (ADR-0155) and turn-based life is a sellable frame if the store page says so.
- Controller/gamepad support, localisation beyond English, Steam achievements, cloud saves, Steamworks SDK integration: a Steam depot ships without any of them.
- The Phase 13 map actors (AVaelenViewActor, AVaelenAtlasActor) in the shipped game: they build a second world instance (VaelenViewActor.cpp:372-396); keep them as dev tools, not a player mode.
- The 256 pins' engine re-recording (09-16 and 09-21 months stay `--no-climate` replays) and any re-freeze not tied to steps 2 and 3.
- The per-task 'failed on purpose' instrument discipline for the menu, mesh, audio and packaging tasks (7, 9, 10): one pinned page or one committed session log each is enough; keep the full discipline for the kernel and the four Phase 19 sittings.
- A second UI technology (UMG/Slate widgets, Blueprints): every screen stays a Canvas page composed kernel-side and digested, which the parse job can see and a screenshot can prove.
- Cosmetic scene work beyond 19.11's row (figure re-placement polish, road meshes, a real settlement square, day/night beyond the sun's arc): after the first sale.

### Risks

- Engine-only defects arrive one per sitting at the owner's cadence (six builds in 19 days; 14.08 cost three fixes, S1 one), and Phase 19 alone budgets four to six sittings before any menu, asset or package exists. => Step 12 (self-hosted UBT compile runner) so compile errors cost a push, not a sitting; batch a Shipping compile into S2; every sitting driven by a fixed `-ExecCmds` script from ENGINE_HANDOFF so a sitting is 30 minutes of typing, not an evening.
- The shipped world is not the premise: today's host plays a free person in a world with no colony and no acting NPCs, and the reputation/judgement machinery of Phase 12 has never been fed. => Steps 2 and 3, headless and in parallel with S1; playtest the opening in the Atlas (`--stand`/`--replay --panel`) before any sitting shows it; the Freedom rule behind a switch first so every existing pin is untouched until the one flip commit.
- Frame time: 28 ms GPU with nothing drawn on the T400, 30.4 ms at ground level over the 13.09 scene, and the T400 is not the customer's card either. => Step 5's renderer config (no Lumen, no Nanite, scalability pinned) measured at S3 against S1's baseline via `check_session.py --baseline p19-s1`; a min-spec second machine at S6; clause (n) target set at 16.7 ms.
- The Game target (monolithic, `*_API` empty) and Shipping configuration have never been linked, and MSVC release is built by no CI job, so a packaged exe could fail to link or replay to different digests. => Shipping compile appended to S2; `windows-msvc-release` added to CI before S6; Session.P19S6 replays the packaged build's stream headlessly as the acceptance.
- Assets in git are undecided (question 5): M_VaelenTile lives on one disk, Content/ is empty, and a walker that is a capsule with no sound is the review's first sentence. => Decide LFS in step 9, commit a minimal set (Mannequin, three CC0 sounds, one map, one material) once; the fallback `VertexColorMaterial` path stays so a clone without LFS still runs.
- The Lively + Colony day turn at 128 may cost more than the 1.5-2 ms measured for Play-only at 256 (12.01's living rate was cut tenfold to make 12.08 affordable). => Measure in step 2 before pinning (`LogVaelenPlay: day ... X ms`, bound 5 ms in release); if over, tune `Living` rate for the played region only, behind the same switch.
- Save safety: the engine store's failure arm was found able to lose a save by name (19.10 audit) and 16.14 has never run on the engine. => S1 runs the 16.14 steps; step 7 closes the `.previous` rename-aside and adds autosave; `Run.Autosave` headless plus the S4 Continue check.
- Re-freezes: steps 2 and 3 touch the played world, and a moved digest in the wrong class silently retires a gate (18.10's CI run 297 lesson: pins written as prose in CMake strings escaped the census). => One flip commit per step with the census diff predicted in writing (ADR-0154's protocol), `run_gates.sh` at the flip heads only, and `ctest -R '^Kernel\.'` after every engine-code change (CI run 309's lesson).
- The gates take 67 minutes and the full CI ten jobs; the interleaved headless tasks could stall the four sittings by pushing engine code the next sitting must not see. => Each sitting checks out its task's commit by subject (the protocol already does); headless tasks that touch engine files land after the sitting they would otherwise contaminate, or behind their switch.
- Scope creep from the ten owner questions (scale, keys, crossing, time passing, start mode) reopening kernel rules mid-phase. => Answer them in one owner reply before S2 (proposed: 250 m tiles at quarter relief; ZQSD+WASD via the key table; explicit M at the fence; day on Space; walker start; StreamHeader era word deferred); anything else is post-sale.

### Estimate

OWNER SITTINGS: 8 planned (S1 close, S2 + Shipping compile, S3, S4 + menu/Continue, S5 assets, S6 package on a second machine, one runner-setup hour, one answer-the-questions sitting) plus 2-4 retakes at the measured rate of 1-3 engine-only defects per build - 10 to 12 sittings of 1-2 h. HEADLESS: about 4-5 weeks of work - step 2 (2-3 days incl. the pin), step 3 (4-5 days incl. switch, flip and re-freeze), step 5 (1 day), step 7 (4-5 days), step 9 prep (1 day), step 10 prep + CI leg (2 days), step 11 (3 days), step 12 workflow (1 day), plus the 19.11/19.12 headless halves already in the plan (3-4 days) and the session logs after each sitting (0.5 day each). CALENDAR: 6-8 weeks if the owner sits twice a week; 10-12 weeks at the pace observed since 09-21 (one sitting per 3-4 days with retakes). ASSUMPTIONS: (a) headless throughput stays at the observed 1-3 numbered tasks per day (352 commits, Phases 13-19 in 20 days); (b) each sitting is scripted and lasts 1-2 h; (c) defects per engine build stay at 1-3 and are repaired headless within a day (the standing rule); (d) no art beyond primitives, vertex colours, the UE Mannequin and three sounds; (e) 128 map only, English only, keyboard and mouse only; (f) the T400 is a floor, not the min spec, and a second Windows machine exists for S6; (g) Steam store page, capsule art, trailer, legal and pricing are the owner's and outside this estimate; (h) the Lively day turn at 128 stays under 5 ms in release; (i) the self-hosted runner is set up in week 1 - without it add 2-3 sittings.

## Proposal 3

### Summary

State read at HEAD 5c985a0 (the task text said 9d1f81b; nine commits landed since: 19.04, 19.05, 19.07, 19.08, 19.09, 19.09b, 19.10, the store fix, and 19.06's headless half). What is real: 14 kernel modules green on 10 CI jobs (248 CTest entries, 18 frozen gates, census 571 sites, check_engine_status/shim_ledger/modules/dll_api/census all clean here today); a 128 world costs 1.0 s to generate in the owner's editor (36.4 s in this Debug Atlas), the ground is 2,097,152 triangles at 128 with 0 steeper than 44.76 deg, and the three scene lines (terrain 105208c54e3c6ea9, layout, sky) print byte-stable. What is NOT real: (1) sitting S1 only half happened - 19.03b COMPILED on the owner's machine (ROADMAP:8191-8194, STATUS:36) but steps 2-6 (Vaelen.View digests, the 16.14 save/load run, Play+Weather screenshot, stat unit baseline, host24 --no-climate control) never came back: no Tests/Run/Sessions/s1-*.log, no s1 row in Tools/engine_builds.txt, and the Session.P19S1 CTest entry does not exist yet (Tests/CMakeLists.txt:140 has Session.SelfTest only); (2) 1,252 engine lines in 21 files since 19.03b (all of Source/VaelenWalk, VaelenWalkController, the subsystem accessors, the 19.10 binding) and 112 shim beliefs have never met a compiler; (3) frame time at ground level on the owner's T400 4GB (D3D11, driver 472.47) is 30.4 ms GPU-bound, and an EMPTY template map with the HUD already costs 28.15 ms - Config/DefaultEngine.ini still carries Phase 00's provisional Lumen GI + Lumen reflections + Nanite + VT + distance fields that Phase 13 said it would decide by ADR and never did; (4) the engine host does not play the game's premise: UVaelenWorldSubsystem::Begin never sets Given.Colony and forces Rules.WantBound = 0 (VaelenWorldSubsystem.cpp:155, :620), so every engine world so far has no mining colony and an unbound start; (5) no packaged build has ever been made (only editor -game), the start is a console command, GameDefaultMap is /Engine/Maps/Entry, Content/ is .gitkeep, /Engine/BasicShapes and /Game/M_VaelenTile are referenced by string path; (6) saves: 16.14 never run on the engine, the store's replace can still lose the last good save by NAME (ROADMAP:8183-8189), format v3 with the migration half of clause (j) deferred, and the log is 84-91% of the image and unbounded. Shortest credible path: finish S1 without rebuilding, run S2 on 19d6a6a, fix the host premise headless, S3, harden the save, S4 (Phase 19 gate), one packaging sitting, two capped content sittings, one long-session soak - ship a 128 map, eight verbs, the walk, the winter, the chronicle, day-on-Space, local saves, prototype look. Nothing on this path moves a WORLD/GEN/HASH census site.

### Order

- 1. S1-bis (owner, no rebuild, on the 19.03b editor already built): steps 2-6 of ENGINE_HANDOFF 'PHASE 19 - sitting S1' plus a frame-time A/B. Bring back Saved/Logs/Vaelen.log with rev-parse first. In the same sitting, at ground level over Vaelen.View 128 120, read stat unit, then type r.DynamicGlobalIlluminationMethod 0, r.ReflectionMethod 0, r.VolumetricCloud 0, r.ScreenPercentage 75, sg.PostProcessQuality 1 and read it again (answers owner question 4 by measurement; the RHI is D3D11 so Lumen may be silently off and the 28 ms may be the Entry map's sky/clouds/post). Headless, in parallel: write Session.P19S1 into Tests/CMakeLists.txt with Tests/Run/Sessions/s1.session (head + step-4 lines against VaelenAtlas --gate <stream> --want-bound 0), a --record s1 row in Tools/engine_builds.txt, STATUS of the 7 files moved by the checker, beliefs -> SEEN. Acceptance: ctest -R '^Session\.P19S1$' green; loaded first: state digest == saved; host24 check line ends --no-climate; python3 Tools/check_engine_status.py counts fewer UNVERIFIED than today's 24; the two stat unit readings written into ROADMAP and, if the A/B wins, DefaultEngine.ini changed with the missing renderer ADR before S3. Why: closes Phase 16's engine half and Phase 18's pending engine line at zero code cost, and retires the frame-time unknown from a measurement instead of a guess.
- 2. S2 (owner, task 19.06, commit 19d6a6a found by subject '19.06: ', budget two sittings): ENGINE_HANDOFF 'sitting S2' as written - build, launch /Engine/Maps/Entry?game=/Script/VaelenWalk.VaelenWalkGameMode -game, Vaelen.Walk 128 120, Vaelen.Probe 64 and the forced 5.0 cm bias, ZQSD lines, the steepest slope, three Space, stat unit at eye level, two screenshots, the Vaelen.View control. Headless side: Session.P19S2 reader, engine_builds row s2, every error reported not repaired -> fix here -> shim corrected -> python3 Tools/test_engine_shim.py && Tools/verify_fast.sh && ctest -R '^Kernel\.' (the CI-309 lesson) -> new commit -> repeat sitting. Fallbacks declared now: PMC -> UStaticMesh built from the same int32 arrays; runtime UInputMappingContext -> a committed IMC asset; GEngine->VertexColorMaterial -> a committed M_VaelenTile. Acceptance: ctest -R '^Session\.P19S2$': terrain line == VaelenAtlas --size 128 --prehistory 300 --years 120 --scene-terrain R byte for byte, climate line byte-equal, probe <= 1.0 cm with 0 misses and the bias printing 5.0, four move lines in ZQSD order. Why: 1,252 lines and 112 beliefs run once before anything is built on them; this is the biggest single unknown left.
- 3. Headless while S2 waits: 19.11's and 19.12's headless halves. AVaelenScenery in Source/VaelenWalk (ISMs from /Engine/BasicShapes for houses/figures/square/roads/pit, rebuilt on OnViewsTaken only), WalkKeys (TWREMFGK) in AVaelenWalkController with M at the fence -> Scene::CrossingOf and Speak/Give/Take -> Scene::AimAt (Tab fallback), the one-line-a-day LogVaelenWalk: day D tile T region R life L looked K, Vaelen.Scene; ReplayPlayed.cmake driver Replay.OnFoot with -DERA=--climate -DMORE='--stream --keys TWREMFGK', Run.Gate.OnFoot as gates 19 and 20 in Tools/run_gates.sh, Session.P19S3/S4 readers, the LogVaelenWalk summary line in Vaelen.Stream.Write. Acceptance: parse 0 errors on 24+ TUs; Kernel.UiFence/UiFenceSelfTest/ModuleLists/EngineStatus/EngineStatusSelfTest/ShimLedger green after every engine-code commit; Tools/run_gates.sh --self-test still red on purpose; frozen_census --diff 19d6a6a HEAD shows 0 removed. Why: S3 and S4 need code that exists only as a plan row today.
- 4. New headless task 19.13 'the world the slice plays' (before S3, so S3/S4 record the shipping world): the host declares the premise. UVaelenWorldSubsystem::Begin sets Given.Colony = true and Rules.WantBound = 1 (VaelenWorldSubsystem.cpp:155 and :620 today force 0; HeadlessCheck already prints --colony at :488 and the Atlas has --colony); Run.Walk (Tests/Run/Test_Walk.cpp) gains a Colony=true, WantBound=1 cell; a Vaelen.Walk that finds nobody bound says so (BeginEnslaved can return 0, ROADMAP:3866). Decide owner questions 2, 3, 8, 9 in one ADR: WalkKeys with Speak on F (AZERTY ZQSD; QWERTY WASD deferred because W is Work - ValidKeys refuses it), M at the fence, the day stays on Space (an optional host timer in VaelenGame/Private calling AdvanceDay(1) every N real minutes is legal - it is outside the fence ROOTS and lands as a recorded DayTurned, ADR-0138 - but default OFF so sittings stay comparable), start mode = the walk (DefaultEngine.ini GlobalDefaultGameMode -> /Script/VaelenWalk.VaelenWalkGameMode). Acceptance: VaelenAtlas --empty --colony --panel prints a bound person's page; Run.Walk colony cell replays Wrong 0 with every look == Life.Region; Kernel.WorldWiring untouched (git diff --quiet d339068 HEAD -- Tools/check_world_wiring.py Source/VaelenRun/Private/Aelvor.cpp); no WORLD pin moves (host options are not kernel changes). Why: a slice that sells 'you start enslaved in a mining colony' cannot be recorded in a world with no colony and a free start.
- 5. S3 (owner, task 19.11, tag p19-s3, budget two sittings): Vaelen.Walk 128 120, Vaelen.Scene, Vaelen.Day 10, Vaelen.Scene, Vaelen.Stream.Write; frost-day and summer screenshots; stat unit at eye level in the settlement with the HUD on and the step-1 renderer config committed; four fence walks printing blocked; the Vaelen.View control. Acceptance: ctest -R '^Session\.P19S3$': VaelenAtlas --replay <S3 stream> --panel --want-bound 1 --stream --keys TWREMFGK --scene --colony prints the Scene, Climate and Play lines byte-identical as files; every LogVaelenWalk day line has region == life == looked; drawn instance counts == the layout line's; the eye-level frame time is not worse than S1-bis's baseline. Why: the drawn scene must be the built scene from the played world (the 14.10/15.10 map was another world instance, VaelenViewActor.cpp:372-396); this is the walkable-world unknown.
- 6. Headless 16.15 'a save that cannot lose its name' + the EA save policy, before S4 so S4 exercises it: rename-aside in both stores (<name>.previous written before the replace, hidden by the name rule like .writing, restored by Read when the name is missing; needs IsUsableCheckpointName in the kernel store, Source/VaelenGame/Private/VaelenCheckpointStore.cpp and the kernel Store), autosave every N DAY TURNS from AdvanceDay (never wall clock, ADR-0109; N default 30), Saves() listing without generating (16.04's manifest). Policy written into ROADMAP/DECISIONS and the future store page: v3 + HOST section is the EA format; saves are per-build until 1.0 (the container header already refuses by version/seed/wiring and names its reason); the golden corpus Run.Golden on MSVC stays the cross-compiler witness; the v4 bump with the forge (clause j) is post-EA. Acceptance: Run.Store gains the cell 'rename fails after delete -> <name>.previous readable, Read restores it, CannotWrite returned'; Atlas.SaveThenLoadFromDisk and Save.AtomicAndComplete unchanged; in S4, Vaelen.Save walk1 / close / Vaelen.Load walk1: loaded state digest == saved and the walker arrives by Scene::Arrival on the played region. Why: the store's failure arm was found by audit in code no build has run; a save that can vanish is the one defect that ends an early-access run.
- 7. S4 (owner, task 19.12, tag p19-s4): 30+ days on foot at 128 in the colony world - every verb once, >= 2 crossings by M at the fence, >= 1 refused (TooFar, nothing recorded), >= 1 Speak aimed at a figure, days into winter, one Vaelen.Save/Load pair, Vaelen.Stream.Write, a screenshot whose last row is the panel digest, stat unit. Headless close: the stream committed as Tests/Run/Streams/aelvor128-onfoot-<date>.stream; Replay.OnFoot, Run.Gate.OnFoot, Session.P19S4; the Phase 19 gate read clause by clause (a)-(n) with the CI run id; ADR-0155..0159 applied; STATUS moved by the checker, not by hand. Acceptance: N of N answered identically, D of D days; both LogVaelenPlay lines byte-identical; P (put back) == 0, C >= 2, A >= 1; Replay.Played/Lived/Climate unchanged; python3 Tools/check_shim_ledger.py --expect-beliefs 0; run_gates.sh GATES-DONE 0 failing (of 20). Why: a life on foot that replays exactly is the proof the walker is not an input; Phase 19 closes here.
- 8. Packaging sitting P1 (owner; new task 22.01 'the build a stranger can run', headless prep first): never done - only editor -game has run. Headless prep: a committed empty project map Content/Maps/Walk.umap with the walk game mode (GameDefaultMap must stop being /Engine/Maps/Entry), DefaultGame.ini +DirectoriesToAlwaysCook for /Engine/BasicShapes (referenced by string at VaelenViewActor.cpp:312 and VaelenAtlasActor.cpp:351 - not cooked otherwise) or a committed copy under /Game, M_VaelenTile committed or its FObjectFinder at VaelenLand.cpp:38 dropped, the start flow in code (AVaelenWalkGameMode::BeginPlay calls Begin(128,120,true) then possesses - a Shipping player has no console), F9 writes the stream and a key writes a save, Git LFS for Content/ (owner question 5, answer yes). Sitting: RunUAT BuildCookRun -project=Vaelen.uproject -platform=Win64 -clientconfig=Development then Shipping -cook -stage -pak -archive; run the archived exe on a machine or user account without the editor; bring back its log. Acceptance: the packaged exe starts into the walk unaided, its LogVaelenScene/LogVaelenClimate lines equal the Atlas's byte for byte, its stream replays headless (Replay.OnFoot driver over that file), the noasserts kernel presets on CI stand in for Shipping's compiled-out asserts, first launch to standing on the ground < 10 s at 128 (Begin is ~1 s in Development on the i5-10400). Why: 'sellable' means an executable, and every hazard here (uncooked engine assets, an engine map as default, console-only start) is invisible to the headless CI.
- 9. Long-session soak (headless, Phase 20 subset, name it 20.01): before selling, measure what a player who plays 3 in-game years does to the process. Atlas --stand/--walk extended to 1,080 days at 128 with Play+Stream+Colony+Climate: ms per day turn and RSS at days 1, 360, 720, 1080, save size per year (22 MB at 20 days today; the log grows 49-285 kB per simulated year and 49 Log().All() sites scan it), the first-day detail-promotion hitch (246 ms at 256). A Run.Soak entry that PRINTS (never asserts, ADR-0109), numbers written into ROADMAP, a ceiling decided (proposal: day turn < 50 ms, RSS < 2 GB, save < 100 MB at 3 years). If the ceiling is exceeded, the PlayerActed cut (54% of a 16.8-million-event log, 17.05) is the first candidate and is scheduled as its own re-freeze commit AFTER the EA build, with a versioned save break. Acceptance: the four figures in git; a recorded decision on whether the log cut precedes EA. Why: the unbounded log is the one known growth nobody has run past 142 days on the engine.
- 10. Start and settings screen (headless, VaelenUI, fenced; owner sees it in P1 or a P2 sitting): today there is no UMG widget and the HUD is 15 Canvas->DrawText rows. Minimum: New / Continue (newest container through Saves()) / Quit, a scalability preset (Low/Medium from step 1's numbers), the key table shown from Panel().Verbs[i].Key (WalkKeys), the ProjectVersion from DefaultGame.ini. Acceptance: Kernel.UiFence and the parse green; Continue reaches the saved state digest; DefaultKeys pins (Test_Panel.cpp:35-36, Atlas.PanelFrozen, Replay.Played's panel 7de2c5faf3cc1813) unchanged. Why: a player cannot type Vaelen.Walk.
- 11. Content C1-C2 (owner, two sittings, hard cap): the prototype look is cylinders, cones and vertex colour. Free Epic content only: the UE5 Mannequin for the walker (and optionally a static-pose Mannequin mesh for figure ISMs), one terrain material reading the vertex colour and the 19.09 snow/grass channels, house meshes from a free pack, a HUD font. Committed under Content/ via Git LFS. Acceptance: screenshots in the sitting log; frozen_census --diff 0 removed (content moves nothing headless); check_engine_status still holds; the S3/S4 frame time not worse by more than the cap the owner sets in step 1. If the cap is hit, ship the prototype look with an honest EA disclosure. Why: the only step whose value cannot be measured headless, so it is last and capped.
- 12. Store minimum (owner, not code, after P1): Steam does not require the Steamworks SDK to run an exe; ship the Shipping package built in step 8 with CompanyName and ProjectVersion set (DefaultGame.ini), a save-policy line on the store page from step 6, min spec from the S3 reading (the owner's i5-10400 + T400 4 GB is the only measured machine and is below a typical min spec; that is the number, not a guess). Acceptance: the archived build uploads and launches from the Steam client on the owner's machine. Why: everything else (Cloud saves, achievements, localisation) is post-sale.

### Cuts

- Wiring VaelenInfrastructure into the played world (owner question 7): a switch-and-flip re-freeze of every WORLD digest plus a HOST byte, and even wired it holds no house (Buildings.h:44-51); houses from families (19.08) are enough for the slice.
- A kernel position below the region for persons, NPCs walking within a day, travel within a day: each changes PersonInfo/state digests or the stream format, or needs the frame clock the fence refuses (check_ui_fence.py:112-115).
- Skeletal animation for NPCs: figures stay ISM shapes (or a static-pose Mannequin); only the player gets the animated Mannequin, in step 11 under a cap.
- Landscape, Nanite, World Partition, Lumen: keep the PMC ground; the step-1 A/B most likely turns Phase 00's provisional renderer settings OFF rather than adding more.
- 256 maps for EA: ship 128 (1.0 s generation on the owner's machine, 99 regions, 0 steep triangles, 2.1 M triangles whole-map at the full lattice); 256 costs 6 s to generate and 8.4 M triangles.
- The Phase 13 map actors (AVaelenViewActor/AVaelenAtlasActor build their own second world): out of the shipped start flow; Vaelen.View stays only as the sittings' control line.
- Log truncation, filling the Cause edge, the v4 format bump with the migration forge (Phase 16 clause j), severing ComputeStateDigest from the trailer: each moves hundreds of the 571 census sites and invalidates saves; one post-EA re-freeze phase with a versioned save break, not a slice task.
- The Lively wiring (Phase 12 living/repute/fame/judgement) in the shipped host: Begin costs 61 s at 128 for 60+30 years with it against ~1 s without; it stays a headless gate world until its cost is measured in step 9.
- Phase 21 MODDING entirely; Phase 22 balance beyond the soak numbers; a save browser with thumbnails; Steam Cloud and achievements; localisation (the panel text is composed in the kernel and a French page is a new WORLD/panel pin per language).
- New verbs, combat, inventory, quests: Panel.h:43 fixes eight verbs; any addition moves every panel pin and the stream format. The slice sells the eight verbs, the walk, the winter and the chronicle.
- Re-recording the 2026-09-16 and 2026-09-21 months in the climate world: they stay --no-climate replays; the first climate-and-colony month is S4's.
- Audio: nothing exists and no phase built any (Phase 13's row names it and delivered none); not before a first sale.
- QWERTY WASD binding (owner question 2): W is Work and ValidKeys refuses the clash; AZERTY ZQSD only for EA, a rebinding screen later.

### Risks

- S2 fails on an engine belief: 1,252 never-compiled lines, 112 shim beliefs (PMC sections with complex collision under CharacterMovement, a mapping context made without an asset, ZQSD following the AZERTY layout, a lit sky in a lightless map, GEngine->VertexColorMaterial, FKey(FName)); 14.08 needed three fixes nothing headless could see. => Budget two sittings for S2 (the plan's six-sitting budget for four tasks); report-not-repair; every error -> headless fix + shim line corrected + test_engine_shim.py + verify_fast + ctest -R '^Kernel\.' + a fresh pinned commit; the three fallbacks (static mesh from the same arrays, a committed IMC asset, a committed M_VaelenTile) are decided before the sitting so a failure redesigns the engine side only; 19.07-19.10 stand headless regardless.
- Frame time on the owner's T400 4 GB (D3D11, driver 472.47): 30.4 ms GPU-bound at ground level and 28.15 ms for an empty template map with the HUD; DefaultEngine.ini enables Lumen GI, Lumen reflections, Nanite, virtual textures and mesh distance fields as a Phase 00 'provisional' that no ADR ever revisited; the '>= 60 fps' of the spine was never a measured possibility. => Step 1's console A/B on the same scene (GI/reflections method 0, volumetric clouds off, screen percentage, post quality) decides by measurement; the winning config is committed with the renderer ADR Phase 13 owed; the near-chunks-at-full-lattice / far-at-stride-8 design in AVaelenLand is the LOD knob (a chunk is 32,768 triangles, the played region touches a handful); the EA min spec is declared from the S3 reading, not from hope.
- The engine host does not play the premise: Begin never sets Given.Colony and forces Rules.WantBound = 0 (VaelenWorldSubsystem.cpp:155, :620), so S3/S4 as planned would record and pin a world with no mining colony and a free start - the opposite of the pitch. => Step 4 (19.13) lands before S3: host Options Colony=true, WantBound=1, a Run.Walk cell for it, HeadlessCheck already prints --colony; Replay.OnFoot then pins the shipping world. Cost is a host option, no kernel digest moves; if BeginEnslaved returns 0 on the 128 seed, the host says 'nobody to be' and the seed or size question goes to the owner before S3, not after.
- Save loss or incompatibility during early access: FVaelenCheckpointStore's replace can still lose the last good save by name after a failed rename (ROADMAP:8183-8189), 16.14 has never run on the engine, the format is v3 with no migration chain (clause j deferred), and every planned kernel change (Infrastructure, Cause edges, log cut) invalidates saves by digest. => Step 6's rename-aside with a Run.Store cell that fails on purpose; S1-bis runs 16.14 and S4 runs Save/Load on a walked world; a written EA policy 'saves are per-build until 1.0' enforced by the container header's version/seed/wiring refusal that names its reason; a tagged format-freeze commit before the EA build; Run.Golden on the MSVC leg as the cross-compiler witness; kernel changes that move WORLD digests are scheduled as versioned events after EA.
- No packaged build has ever existed: engine assets referenced by string path (/Engine/BasicShapes, /Engine/Maps/Entry as GameDefaultMap, /Game/M_VaelenTile on one disk) are not cooked by themselves, the start is a console command, Shipping compiles asserts out, and Content/ is a .gitkeep. => Step 8 as its own sitting with the headless prep listed (project map committed, DirectoriesToAlwaysCook or /Game copies, start flow in AVaelenWalkGameMode::BeginPlay, Git LFS); acceptance is the exe running without the editor and its scene lines matching the Atlas; the noasserts CI presets already model compiled-out asserts.
- Long-session growth: the event log is 84-91% of the image and unbounded (49-285 kB per simulated year), 49 Log().All() call sites with a dozen scanning it per tick, the first-day detail promotion is a 246 ms hitch at 256, and nobody has run the engine past 142 days. => Step 9's Run.Soak prints ms/day, RSS and save size at 1/360/720/1080 days at 128 with the shipping wiring and a ceiling is decided in git; if exceeded, the PlayerActed cut (54% of the log) is scheduled as a post-EA re-freeze with a versioned save break; autosave cadence in day turns keeps a player's loss bounded.
- Content honesty: shapes and vertex colours are not a sellable look; asset work cannot be verified headless and its cost is open-ended. => Free Epic content only (Mannequin, starter meshes), two capped sittings, screenshots as the acceptance, census --diff 0 removed as the proof nothing frozen moved; if the cap is hit, ship the prototype look with an EA disclosure rather than slip.
- Process drift on the engine side: self-tests with known answers went stale twice this week (CI runs 306 and 309), STATUS claimed more than was built for five files until 19.01, and the sitting protocol is one machine, one person, serial. => ctest -R '^Kernel\.' after every engine-code commit (thirty seconds); a read-only audit fleet before each sitting (the 19.10 audit confirmed one real defect and refuted one predicted error against the owner's own build); every sitting pinned to a commit found by subject, its log committed and re-read by a Session.* entry; S1-bis needs no rebuild, so it costs one short sitting.
- Determinism across the engine boundary in the walk: float capsule positions meet integer fences (Scene::Inside, RegionAt) every step, and a look recorded from the wrong region changes the world (ROADMAP:4955). => 19.07's Run.Walk already proves the body is not an input headless (two wanderings, one stream byte for byte); S3/S4 require region == life == looked on every day line and P == 0 put-backs; the probe's forced 5.0 cm bias proves the probe can fail.
- The task text's HEAD (9d1f81b) and the branch (5c985a0) differ by nine commits; a sitting that builds 'HEAD' instead of the named commit gets several possible first causes. => Every sitting checks out the commit by subject as ENGINE_HANDOFF step 0 says (S1-bis: no rebuild at all; S2: '19.06: ' = 19d6a6a); the FIRST line of every log is git rev-parse HEAD and the reader refuses a log without it.

### Estimate

Owner sittings: planned 7, budget 10 - S1-bis (1, ~1 h, no rebuild), S2 (1, budget 2), S3 (1, budget 2), S4 (1), packaging P1 (1, budget 2), content C1-C2 (2, capped). The budget rule is the Phase 19 plan's own: a failed build is a repeat sitting of the same task (14.08 took three invisible fixes). At the observed cadence (five builds between 09-07 and 09-21, S1's first attempt the day 19.03 landed) that is about two sittings a week, so 4-5 calendar weeks on the critical path S1-bis -> S2 -> (19.13) -> S3 -> (16.15) -> S4 -> P1 -> C1/C2, each gated on the previous log. Headless weeks: ~5.5 in total, mostly overlapped with the sittings - 19.11+19.12 halves 1.0, 19.13 host premise + Run.Walk colony cell + the ADR on questions 2/3/8/9 0.5, 16.15 rename-aside + autosave cadence + Saves listing + EA save policy 1.0, 22.01 packaging prep (project map, cook list, start flow in code, LFS) 0.5, start/settings screen in VaelenUI 1.0, 20.01 soak 1.0, gate reads/ADRs/STATUS/ENGINE_HANDOFF 0.5. Calendar to a packaged, walkable, savable slice at 128 with the prototype look: 6-8 weeks; plus 2-3 weeks if the content cap is spent in full. Assumptions: the owner's i5-10400 + T400 4 GB (D3D11) stays the only engine machine and becomes the declared min spec; the shipped host wiring is Play+Stream+Colony+Climate without Lively (Begin ~1 s at 128 in Development; this Debug Atlas took 36.4 s and 38.5 MiB for the same world, consistent with the ~30x debug multiple); 128 maps only; the eight verbs and day-on-Space are the game; zero WORLD/GEN/HASH census sites move before the EA build (every step's acceptance includes frozen_census --diff 0 removed), so no re-freeze and no save break happen on this path; the 10-job CI matrix (248 entries) stays green throughout; each sitting is a written script of <= 2 h; headless capacity is not the bottleneck, the owner's chair is. Not estimated: art beyond free Epic content, audio (none exists), localisation, Steam features, and any kernel change to the log or the causal graph - those are the post-EA re-freeze phase.
